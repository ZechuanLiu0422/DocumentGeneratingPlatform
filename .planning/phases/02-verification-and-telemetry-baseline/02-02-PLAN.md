---
phase: 02-verification-and-telemetry-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/seed-phase-2.mjs
  - tests/phase-02/auth-rls/supabase-auth-helpers.ts
  - tests/phase-02/auth-rls/auth-rls.test.ts
autonomous: true
requirements:
  - SAFE-02
must_haves:
  truths:
    - "Maintainer can run a deterministic local Supabase regression suite that proves one authenticated user cannot read, update, or delete another user's drafting data."
    - "The SAFE-02 proof uses real local Supabase auth plus RLS-enforced anon clients, not service-role-only shortcuts, per the research constraint."
    - "Cross-user isolation is proven across the core drafting tables that matter to workflow integrity and future refactors."
  artifacts:
    - path: "package.json"
      provides: "Named RLS regression scripts for seeding and execution"
      contains: "repo-local scripts that reset local Supabase, seed fixtures, and run SAFE-02 tests without relying on a globally installed CLI"
    - path: "package-lock.json"
      provides: "Deterministic dependency lockfile updates for local Supabase tooling"
      contains: "the resolved graph for any new SAFE-02 test tooling"
    - path: "scripts/seed-phase-2.mjs"
      provides: "Deterministic local Supabase seed flow for SAFE-02 and SAFE-03"
      contains: "two-user fixture creation and seeded draft/workflow rows"
    - path: "tests/phase-02/auth-rls/supabase-auth-helpers.ts"
      provides: "Helpers for obtaining real authenticated anon clients against local Supabase"
      contains: "session sign-in and user-scoped client creation"
    - path: "tests/phase-02/auth-rls/auth-rls.test.ts"
      provides: "Executable multi-user RLS regression coverage"
      contains: "read/update/delete isolation checks across drafting tables"
  key_links:
    - from: "tests/phase-02/auth-rls/auth-rls.test.ts"
      to: "scripts/seed-phase-2.mjs"
      via: "shared deterministic fixture ids and user credentials"
      pattern: "phase-2|seed"
    - from: "tests/phase-02/auth-rls/auth-rls.test.ts"
      to: "supabase/migrations/20260320120000_initial_schema.sql"
      via: "real RLS policy enforcement"
      pattern: "policy|auth.uid|user_id"
    - from: "tests/phase-02/auth-rls/auth-rls.test.ts"
      to: "supabase/migrations/20260321110000_collaborative_writing_upgrade.sql"
      via: "drafting tables and policy coverage"
      pattern: "drafts|document_versions|writing_rules|reference_assets"
    - from: "tests/phase-02/auth-rls/auth-rls.test.ts"
      to: "tests/phase-02/auth-rls/supabase-auth-helpers.ts"
      via: "authenticated anon client creation"
      pattern: "createClient|signInWithPassword"
---

<objective>
Create a real Supabase auth/RLS regression harness that proves user isolation with authenticated anon clients instead of service-role shortcuts.

Purpose: SAFE-02 is the highest-value brownfield proof for data isolation because Supabase RLS is the real authorization boundary in this app.
Output: Local seeding utilities, authenticated anon-client helpers, and an executable regression suite that demonstrates cross-user reads and mutations are blocked.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@package.json
@supabase/config.toml
@supabase/migrations/20260320120000_initial_schema.sql
@supabase/migrations/20260321110000_collaborative_writing_upgrade.sql
@supabase/migrations/20260323150000_outline_planning_upgrade.sql
@scripts/check-supabase-schema.mjs

<interfaces>
From `scripts/check-supabase-schema.mjs`:

```javascript
const client = createClient(url, serviceRoleKey, {
  auth: { persistSession: false, autoRefreshToken: false },
});
```

From the research constraints:

```text
SAFE-02 must use real local Supabase + authenticated anon clients; service-role-only checks are invalid proof.
```

Representative tables called out by research:

```text
drafts
document_versions
documents
writing_rules
reference_assets
contacts
common_phrases
usage_events
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed deterministic multi-user fixtures against the local Supabase stack</name>
  <files>package.json, scripts/seed-phase-2.mjs, tests/phase-02/auth-rls/supabase-auth-helpers.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
    - package.json
    - supabase/config.toml
    - supabase/migrations/20260320120000_initial_schema.sql
    - supabase/migrations/20260321110000_collaborative_writing_upgrade.sql
    - supabase/migrations/20260323150000_outline_planning_upgrade.sql
    - scripts/check-supabase-schema.mjs
  </read_first>
  <behavior>
    - Test 1: the seed flow creates at least two known users plus stable drafting rows for each user that later tests can target deterministically.
    - Test 2: authenticated anon-client helpers can sign in as each seeded user and return user-scoped clients for assertions.
  </behavior>
  <action>Create `scripts/seed-phase-2.mjs` as the shared deterministic fixture entrypoint for SAFE-02 and SAFE-03. Use the service role only for setup: ensure two seeded users exist, assign stable emails/passwords for local-only testing, and insert per-user rows into `drafts`, `document_versions`, `documents`, `writing_rules`, `reference_assets`, `contacts`, `common_phrases`, and `usage_events`. Then add `tests/phase-02/auth-rls/supabase-auth-helpers.ts` with helpers that sign in through the local Supabase auth API and return authenticated anon clients whose requests are subject to real RLS. Add package scripts such as `test:phase-02:rls:reset`, `test:phase-02:rls:seed`, and `test:phase-02:rls` so verification uses repo-local commands (`npx supabase` or npm scripts), not an assumed global CLI. Do not blur setup and proof into one service-role-only command.</action>
  <acceptance_criteria>
    - The seed script is idempotent enough to run after `npx supabase db reset --local` or an equivalent repo-local npm script.
    - Seeded fixture ids and credentials are explicit and reusable by SAFE-02 and SAFE-03.
    - Auth helpers produce user-scoped anon clients for assertions rather than service-role clients.
    - The setup path clearly separates seeding from the actual RLS proof step.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:phase-02:rls:reset && npm run test:phase-02:rls:seed</automated>
  </verify>
  <done>Phase 2 has a deterministic local Supabase fixture harness that can prove auth isolation on the real database and seed later browser smoke data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove cross-user isolation with authenticated anon clients on real RLS policies</name>
  <files>tests/phase-02/auth-rls/auth-rls.test.ts, tests/phase-02/auth-rls/supabase-auth-helpers.ts, scripts/seed-phase-2.mjs, package.json</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
    - tests/phase-02/auth-rls/supabase-auth-helpers.ts
    - scripts/seed-phase-2.mjs
    - supabase/migrations/20260320120000_initial_schema.sql
    - supabase/migrations/20260321110000_collaborative_writing_upgrade.sql
    - supabase/migrations/20260323150000_outline_planning_upgrade.sql
  </read_first>
  <behavior>
    - Test 1: user A cannot read user B's rows from `drafts`, `document_versions`, `documents`, `writing_rules`, `reference_assets`, `contacts`, `common_phrases`, or `usage_events`.
    - Test 2: user A cannot update or delete user B's drafting rows, while user A can still read and mutate their own seeded rows.
    - Test 3: the assertions are executed through authenticated anon clients, so a broken policy actually fails the suite.
  </behavior>
  <action>Write `tests/phase-02/auth-rls/auth-rls.test.ts` as a real regression suite against the local Supabase stack. For each seeded user, sign in through the local auth boundary, obtain authenticated anon clients, and assert both positive ownership behavior and negative cross-user isolation behavior. Cover the drafting tables that matter to brownfield safety first: `drafts`, `document_versions`, `documents`, `writing_rules`, `reference_assets`, `contacts`, `common_phrases`, and `usage_events`. Do not accept service-role-only checks as proof. If a table requires a setup-only service-role read to confirm the fixture id, do that in the seed script, not in the assertions.</action>
  <acceptance_criteria>
    - SAFE-02 proof is executed through authenticated anon clients only.
    - The suite covers both read isolation and mutation isolation, not just empty selects.
    - The regression suite demonstrates self-owned access still works for seeded data.
    - The package scripts for SAFE-02 are explicit enough for CI and local reruns without depending on a globally installed Supabase CLI.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:phase-02:rls:reset && npm run test:phase-02:rls:seed && npm run test:phase-02:rls</automated>
    <automated>npm run check:schema</automated>
  </verify>
  <done>Maintainers have executable proof that one user cannot read or mutate another user's drafting data through the real Supabase auth and policy boundary.</done>
</task>

</tasks>

<verification>
Reset the local Supabase stack, seed the two-user fixtures, then run the SAFE-02 regression suite and the existing schema smoke check. Inspect failures as policy regressions first, not as test harness issues.
</verification>

<success_criteria>
SAFE-02 is satisfied when the repo can deterministically prove cross-user isolation on the real local Supabase stack using authenticated anon clients and seeded multi-user data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-verification-and-telemetry-baseline/02-02-SUMMARY.md`
</output>
