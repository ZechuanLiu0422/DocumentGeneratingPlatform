---
phase: 02-verification-and-telemetry-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .github/workflows/phase-02-safety.yml
  - tests/phase-02/contracts/shared-fixtures.ts
  - tests/phase-02/contracts/route-contract-helpers.ts
  - tests/phase-02/contracts/harness-smoke.test.ts
  - tests/phase-02/contracts/validation-contract.test.ts
  - tests/phase-02/contracts/persistence-contract.test.ts
  - tests/phase-02/contracts/workflow-route-contract.test.ts
autonomous: true
requirements:
  - SAFE-01
must_haves:
  truths:
    - "Maintainer can run one fast `node:test` command in CI that proves validation contracts, persistence helper shaping, and core workflow route contracts without booting the browser or calling live AI providers."
    - "Workflow route-contract tests fail when auth gates, validation, or response-shape guarantees regress on the protected drafting routes."
    - "The Phase 2 test baseline extends the existing Phase 1 Node runner instead of introducing new runner churn, per research guidance."
  artifacts:
    - path: "package.json"
      provides: "Named local and CI test scripts for the Phase 2 contract suite"
      contains: "repo-local scripts for SAFE-01 contracts and a CI-safe aggregate entrypoint"
    - path: "package-lock.json"
      provides: "Deterministic dependency lockfile updates for new Phase 2 tooling"
      contains: "the resolved dependency graph for any new Phase 2 test-only packages"
    - path: ".github/workflows/phase-02-safety.yml"
      provides: "CI wiring for the Phase 2 safety baseline"
      contains: "workflow steps that install dependencies and run the SAFE-01 contract suite on pushes and pull requests"
    - path: "tests/phase-02/contracts/shared-fixtures.ts"
      provides: "Deterministic workflow and draft fixtures reused across contract tests"
      contains: "fixture builders for route payloads and stored draft rows"
    - path: "tests/phase-02/contracts/route-contract-helpers.ts"
      provides: "Test helpers for direct Next route invocation and boundary stubbing"
      contains: "request builders and temporary module-boundary mocks"
    - path: "tests/phase-02/contracts/harness-smoke.test.ts"
      provides: "Executable proof that the Phase 2 contract harness is wired before deeper assertions land"
      contains: "node:test checks for package scripts, CI workflow coverage, and helper importability"
    - path: "tests/phase-02/contracts/validation-contract.test.ts"
      provides: "Executable validation coverage for core request schemas"
      contains: "node:test assertions for route input and export payload contracts"
    - path: "tests/phase-02/contracts/persistence-contract.test.ts"
      provides: "Executable helper coverage for persistence-state normalization"
      contains: "node:test assertions for draft/version shaping helpers"
    - path: "tests/phase-02/contracts/workflow-route-contract.test.ts"
      provides: "Executable route-contract coverage for the staged workflow endpoints"
      contains: "direct route handler assertions for auth, bad input, and success payload shape"
  key_links:
    - from: "tests/phase-02/contracts/validation-contract.test.ts"
      to: "lib/validation.ts"
      via: "schema parse and rejection assertions"
      pattern: "Schema|parse|safeParse"
    - from: "tests/phase-02/contracts/persistence-contract.test.ts"
      to: "lib/collaborative-store.ts"
      via: "draft/version normalization contract assertions"
      pattern: "normalize|DraftRecord|VersionRecord|saveDraftState"
    - from: "tests/phase-02/contracts/workflow-route-contract.test.ts"
      to: "app/api/ai/intake/route.ts"
      via: "direct POST invocation"
      pattern: "POST\\("
    - from: "tests/phase-02/contracts/workflow-route-contract.test.ts"
      to: "app/api/ai/outline/route.ts"
      via: "direct POST invocation"
      pattern: "POST\\("
    - from: "tests/phase-02/contracts/workflow-route-contract.test.ts"
      to: "app/api/ai/draft/route.ts"
      via: "direct POST invocation"
      pattern: "POST\\("
    - from: "tests/phase-02/contracts/workflow-route-contract.test.ts"
      to: "app/api/ai/review/route.ts"
      via: "direct POST invocation"
      pattern: "POST\\("
---

<objective>
Establish the SAFE-01 baseline on the existing `node:test` runner so maintainers can prove route contracts, persistence helpers, and validation boundaries in CI without introducing framework churn.

Purpose: This is the lowest-risk brownfield safety rail for refactoring because it tests the current server seams directly and stays aligned with the already-shipping Phase 1 test style.
Output: Named `node:test` scripts, CI wiring, shared contract fixtures/helpers, and a focused contract suite covering validation, persistence shaping, and direct workflow route handler behavior.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@package.json
@lib/validation.ts
@lib/collaborative-store.ts
@lib/api.ts
@app/api/ai/intake/route.ts
@app/api/ai/outline-plan/route.ts
@app/api/ai/outline/route.ts
@app/api/ai/draft/route.ts
@app/api/ai/review/route.ts
@app/api/generate/route.ts
@tests/phase-01/draft-save-contract.test.ts

<interfaces>
From `package.json`:

```json
{
  "scripts": {
    "build": "next build",
    "lint": "next lint",
    "check:deploy": "node --env-file=.env scripts/check-deploy.mjs",
    "check:schema": "node --env-file=.env.local scripts/check-supabase-schema.mjs"
  }
}
```

From `lib/api.ts`:

```typescript
export class AppError extends Error { ... }
export function createRequestContext(request: NextRequest, route: string, meta: Record<string, unknown> = {}): RequestContext
export function handleRouteError(error: unknown, context: RequestContext)
export function ok<T>(context: RequestContext, payload: T, status = 200)
```

From the workflow routes:

```typescript
export async function POST(request: NextRequest) { ... }
```

From the existing Phase 1 test style:

```typescript
import test from 'node:test';
import assert from 'node:assert/strict';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the reusable `node:test` contract harness for Phase 2</name>
  <files>package.json, package-lock.json, .github/workflows/phase-02-safety.yml, tests/phase-02/contracts/shared-fixtures.ts, tests/phase-02/contracts/route-contract-helpers.ts, tests/phase-02/contracts/harness-smoke.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
    - package.json
    - package-lock.json
    - tests/phase-01/draft-save-contract.test.ts
    - app/api/ai/intake/route.ts
    - lib/api.ts
  </read_first>
  <behavior>
    - Test 1: maintainers can invoke a repo-local named script for the SAFE-01 contract suite without reaching for a new runner.
    - Test 2: CI wiring exists for the SAFE-01 contract suite so the roadmap's "in CI" requirement is satisfied beyond local reruns.
    - Test 3: route-contract helpers can build `NextRequest` inputs and isolate auth/quota/provider boundaries without booting the full app server.
  </behavior>
  <action>Create the Phase 2 contract harness around the existing Node built-in runner per research constraint SAFE-01. Add package scripts such as `test:phase-02:contracts`, a focused `test:phase-02:contracts:harness` smoke, and a CI-safe aggregate entrypoint, but do not introduce Vitest or migrate Phase 1 tests. Add `.github/workflows/phase-02-safety.yml` so the contract suite actually runs in CI on push and pull request. Add `tests/phase-02/contracts/shared-fixtures.ts` with deterministic payload/builders for intake, outline, draft, review, and export request bodies plus representative stored draft/version rows. Add `tests/phase-02/contracts/route-contract-helpers.ts` with request builders and scoped boundary stubs so direct route-handler tests can assert request parsing, auth gating, and response shape without network, live Supabase, or AI calls. Add `tests/phase-02/contracts/harness-smoke.test.ts` so Task 1 has an executable proof path before the deeper contract assertions from Task 2 exist.</action>
  <acceptance_criteria>
    - `package.json` contains a dedicated SAFE-01 script built on `node --experimental-strip-types --test`.
    - `.github/workflows/phase-02-safety.yml` runs the SAFE-01 contract suite in CI on repository changes.
    - No new test runner dependency is added in this phase, per the research recommendation.
    - Shared fixtures cover both route input payloads and stored drafting rows so later tasks do not duplicate brittle inline objects.
    - Route helpers are generic enough to reuse across intake/outline/draft/review/export contract tests.
    - `tests/phase-02/contracts/harness-smoke.test.ts` can pass immediately after Task 1 without depending on the deeper contract tests from Task 2.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:phase-02:contracts:harness</automated>
    <automated>rg -n "test:phase-02:contracts" .github/workflows/phase-02-safety.yml</automated>
  </verify>
  <done>The repo has a reusable, low-friction contract-test entrypoint that matches the current brownfield stack instead of churning the runner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove validation, persistence, and workflow route contracts with executable tests</name>
  <files>tests/phase-02/contracts/validation-contract.test.ts, tests/phase-02/contracts/persistence-contract.test.ts, tests/phase-02/contracts/workflow-route-contract.test.ts, tests/phase-02/contracts/shared-fixtures.ts, tests/phase-02/contracts/route-contract-helpers.ts, tests/phase-02/contracts/harness-smoke.test.ts, package.json, .github/workflows/phase-02-safety.yml</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
    - lib/validation.ts
    - lib/collaborative-store.ts
    - app/api/ai/intake/route.ts
    - app/api/ai/outline-plan/route.ts
    - app/api/ai/outline/route.ts
    - app/api/ai/draft/route.ts
    - app/api/ai/review/route.ts
    - app/api/generate/route.ts
    - tests/phase-02/contracts/shared-fixtures.ts
    - tests/phase-02/contracts/route-contract-helpers.ts
  </read_first>
  <behavior>
    - Test 1: validation contracts reject malformed route payloads and accept representative valid payloads for core workflow and export requests.
    - Test 2: persistence helper tests prove draft/version normalization and saved-state shaping remain coherent for existing JSON-heavy draft rows.
    - Test 3: direct workflow route tests prove unauthorized requests fail fast, bad payloads normalize through the shared error helper, and success responses return the expected JSON contract when boundaries are stubbed.
  </behavior>
  <action>Write three focused `node:test` files instead of one oversized suite. `validation-contract.test.ts` should cover the request schemas most likely to break future refactors: draft save, intake, outline plan, outline generation, draft generation, review, and export. `persistence-contract.test.ts` should cover the JSON-heavy helper seams the concerns audit flagged as fragile, especially normalized draft/version rows and authoritative saved-state shaping. `workflow-route-contract.test.ts` should invoke exported route handlers directly for the staged workflow routes and export route, stubbing auth/quota/provider boundaries while asserting status codes, normalized error payloads, and minimum success-shape guarantees. Keep the suite brownfield-safe: no browser, no live provider calls, and no real database access in SAFE-01.</action>
  <acceptance_criteria>
    - SAFE-01 has executable coverage for validation, persistence helpers, and route contracts in one CI-friendly script that is also wired into repository CI.
    - Route tests exercise the real exported handlers rather than duplicating route logic in fake wrappers.
    - The suite observes the real `AppError` and `handleRouteError(...)` behavior instead of bypassing it.
    - No test requires provider secrets, a running browser, or live Supabase network access.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:phase-02:contracts</automated>
    <automated>npm run build</automated>
  </verify>
  <done>SAFE-01 is satisfied with fast, executable proof that the core route and helper contracts still hold before maintainers attempt larger refactors.</done>
</task>

</tasks>

<verification>
Run the SAFE-01 harness smoke, then the SAFE-01 contract script and a full production build. Confirm the suite covers validation, persistence, and route contracts without browser startup, live AI traffic, or real database calls, and confirm CI invokes the same entrypoint.
</verification>

<success_criteria>
Maintainers can run one `node:test` contract suite in CI that catches workflow-route, validation, and persistence regressions before browser or database-level tests run.
</success_criteria>

<output>
After completion, create `.planning/phases/02-verification-and-telemetry-baseline/02-01-SUMMARY.md`
</output>
