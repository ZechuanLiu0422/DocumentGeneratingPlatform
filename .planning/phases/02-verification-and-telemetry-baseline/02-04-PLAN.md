---
phase: 02-verification-and-telemetry-baseline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - lib/api.ts
  - app/api/health/route.ts
  - app/api/ai/intake/route.ts
  - app/api/ai/outline-plan/route.ts
  - app/api/ai/outline/route.ts
  - app/api/ai/draft/route.ts
  - app/api/ai/review/route.ts
  - app/api/ai/revise/route.ts
  - app/api/generate/route.ts
  - tests/phase-02/telemetry/telemetry-contract.test.ts
  - tests/phase-02/telemetry/workflow-route-telemetry.test.ts
  - tests/phase-02/telemetry/health-contract.test.ts
autonomous: true
requirements:
  - SAFE-04
must_haves:
  truths:
    - "Operator can inspect structured JSON logs and identify the affected draft, workflow action/stage, provider failure class, and export timing/size for the most important workflow and export paths."
    - "Health inspection no longer leaks avoidable provider/config detail while still telling operators whether the app is alive and degraded."
    - "Telemetry proof extends the existing `lib/api.ts` JSON logging pattern instead of introducing a new observability system, per research guidance."
  artifacts:
    - path: "lib/api.ts"
      provides: "Structured request-log helpers with stable workflow/export metadata fields"
      contains: "a pure log payload builder or equivalent reusable telemetry helper"
    - path: "app/api/health/route.ts"
      provides: "A reduced-scope health contract"
      exports:
        - "GET"
    - path: "app/api/generate/route.ts"
      provides: "Explicit export telemetry for duration, size, and failure classification"
      exports:
        - "POST"
    - path: "app/api/ai/intake/route.ts"
      provides: "Workflow telemetry adoption for intake"
      exports:
        - "POST"
    - path: "app/api/ai/outline-plan/route.ts"
      provides: "Workflow telemetry adoption for planning"
      exports:
        - "POST"
    - path: "app/api/ai/outline/route.ts"
      provides: "Workflow telemetry adoption for outline generation"
      exports:
        - "POST"
    - path: "app/api/ai/draft/route.ts"
      provides: "Workflow telemetry adoption for draft generation"
      exports:
        - "POST"
    - path: "app/api/ai/review/route.ts"
      provides: "Workflow telemetry adoption for review"
      exports:
        - "POST"
    - path: "app/api/ai/revise/route.ts"
      provides: "Workflow telemetry adoption for revise"
      exports:
        - "POST"
    - path: "tests/phase-02/telemetry/telemetry-contract.test.ts"
      provides: "Executable log-shape coverage for workflow and export telemetry"
      contains: "node:test assertions for structured metadata and error classification"
    - path: "tests/phase-02/telemetry/workflow-route-telemetry.test.ts"
      provides: "Executable route-adoption coverage for workflow and export telemetry"
      contains: "direct route handler assertions that required telemetry metadata is attached by the target routes"
    - path: "tests/phase-02/telemetry/health-contract.test.ts"
      provides: "Executable health-response contract coverage"
      contains: "node:test assertions for reduced health payload exposure"
  key_links:
    - from: "tests/phase-02/telemetry/telemetry-contract.test.ts"
      to: "lib/api.ts"
      via: "structured payload-building assertions"
      pattern: "logRequestResult|createRequestContext|build"
    - from: "app/api/ai/draft/route.ts"
      to: "lib/api.ts"
      via: "workflow metadata passed into request logging"
      pattern: "createRequestContext|logRequestResult"
    - from: "app/api/generate/route.ts"
      to: "lib/api.ts"
      via: "export metadata and provider failure classification"
      pattern: "createRequestContext|logRequestResult"
    - from: "tests/phase-02/telemetry/health-contract.test.ts"
      to: "app/api/health/route.ts"
      via: "health payload assertions"
      pattern: "GET\\("
    - from: "tests/phase-02/telemetry/workflow-route-telemetry.test.ts"
      to: "app/api/generate/route.ts"
      via: "route-level telemetry metadata assertions"
      pattern: "createRequestContext|logRequestResult"
---

<objective>
Harden the telemetry and health baseline by extending the existing structured JSON logs with workflow/export metadata and reducing unnecessary health endpoint exposure.

Purpose: SAFE-04 should give operators actionable diagnostics before Phase 3 and Phase 5 refactors increase surface area, without introducing a new observability system into this brownfield app.
Output: Structured telemetry helpers/tests, workflow/export route adoption, and a tighter health contract with executable coverage.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@package.json
@lib/api.ts
@app/api/health/route.ts
@app/api/ai/intake/route.ts
@app/api/ai/outline-plan/route.ts
@app/api/ai/outline/route.ts
@app/api/ai/draft/route.ts
@app/api/ai/review/route.ts
@app/api/ai/revise/route.ts
@app/api/generate/route.ts
@lib/official-document-ai.ts

<interfaces>
From `lib/api.ts`:

```typescript
export function logRequestResult(
  context: RequestContext,
  status: number,
  extra: Record<string, unknown> = {}
) {
  console.log(JSON.stringify({
    level: status >= 500 ? 'error' : 'info',
    request_id: context.requestId,
    route: context.route,
    user_id: context.userId,
    provider: context.provider,
    ip: context.ip,
    status,
    duration_ms: durationMs,
    ...extra,
  }));
}
```

From `app/api/health/route.ts`:

```typescript
return ok(context, {
  status,
  enabledProviders,
  checks: {
    publicEnv: missingPublicEnv.length === 0,
    aiProviderConfigured: enabledProviders.length > 0,
  },
  missingPublicEnv,
});
```

Representative provider failure codes already emitted by `lib/official-document-ai.ts`:

```typescript
AI_AUTH_FAILED
AI_RATE_LIMITED
AI_TIMEOUT
AI_REQUEST_FAILED
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend `lib/api.ts` log shaping with stable workflow and export metadata</name>
  <files>lib/api.ts, tests/phase-02/telemetry/telemetry-contract.test.ts, package.json</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
    - lib/api.ts
    - lib/official-document-ai.ts
    - app/api/generate/route.ts
  </read_first>
  <behavior>
    - Test 1: structured telemetry includes stable workflow/export fields such as `draft_id`, `doc_type`, `workflow_action`, `workflow_stage`, `error_code`, and `export_size_bytes` when available.
    - Test 2: provider failures are classified from existing app error codes rather than logged as undifferentiated 500s.
  </behavior>
  <action>Keep telemetry log-first and extend the existing `lib/api.ts` pattern instead of adding a vendor SDK. Extract a pure log payload builder if that makes testing cleaner, then expand the structured payload to support the metadata operators actually need from SAFE-04: `draft_id`, `doc_type`, `workflow_action`, `workflow_stage`, `error_code`, provider failure classification derived from existing `AI_*` codes, and export-specific fields like `export_size_bytes`. Add a focused `node:test` suite that proves the shaped JSON payloads for success and failure cases without relying on console-snapshot brittleness.</action>
  <acceptance_criteria>
    - Telemetry remains centered on structured JSON logs from `lib/api.ts`.
    - The new log schema is testable without monkey-patching the full app server.
    - Provider failure kinds reuse existing error codes instead of inventing parallel taxonomy.
    - The telemetry test file proves both success and failure payload shapes.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-02/telemetry/telemetry-contract.test.ts</automated>
  </verify>
  <done>The repo has an executable structured-log contract that exposes the missing workflow/export metadata operators need for diagnosis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adopt the telemetry schema in workflow/export routes and tighten the health contract</name>
  <files>app/api/health/route.ts, app/api/ai/intake/route.ts, app/api/ai/outline-plan/route.ts, app/api/ai/outline/route.ts, app/api/ai/draft/route.ts, app/api/ai/review/route.ts, app/api/ai/revise/route.ts, app/api/generate/route.ts, lib/api.ts, tests/phase-02/telemetry/telemetry-contract.test.ts, tests/phase-02/telemetry/workflow-route-telemetry.test.ts, tests/phase-02/telemetry/health-contract.test.ts, package.json</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
    - app/api/health/route.ts
    - app/api/ai/intake/route.ts
    - app/api/ai/outline-plan/route.ts
    - app/api/ai/outline/route.ts
    - app/api/ai/draft/route.ts
    - app/api/ai/review/route.ts
    - app/api/ai/revise/route.ts
    - app/api/generate/route.ts
    - lib/api.ts
  </read_first>
  <behavior>
    - Test 1: workflow and export routes pass enough context into the shared logging path that operators can link failures to a draft, stage, and action.
    - Test 2: route-level telemetry checks fail if one of the target workflow/export routes forgets to attach required metadata such as `draft_id`, `workflow_stage`, `workflow_action`, or export metrics.
    - Test 3: `/api/health` returns a reduced liveness/degraded contract instead of provider inventory or unnecessary config detail.
  </behavior>
  <action>Adopt the expanded telemetry fields in the main workflow routes and export route by passing draft/stage/action/provider context into `createRequestContext(...)` and success/failure log extras. Prioritize the routes that represent workflow progression and export troubleshooting: intake, outline planning, outline generation, draft generation, review, revise, and export. In `app/api/generate/route.ts`, include explicit export timing and size metadata and ensure failure logs surface provider/error classification from the shared helper. Add `tests/phase-02/telemetry/workflow-route-telemetry.test.ts` to invoke the target routes directly and assert they actually attach the required metadata fields instead of only relying on the shared log builder contract. In `app/api/health/route.ts`, reduce the payload to the minimum operator-safe contract needed for SAFE-04, removing public provider inventory if possible while still indicating `ok` vs `degraded` and coarse check states. Add `health-contract.test.ts` so this endpoint cannot drift back toward metadata leakage.</action>
  <acceptance_criteria>
    - The workflow and export routes adopt the shared telemetry schema rather than inventing route-local log formats.
    - Export logs include timing/size fields needed to diagnose performance and failure cases.
    - The health route no longer enumerates enabled provider details or unnecessary public-env exposure.
    - `tests/phase-02/telemetry/workflow-route-telemetry.test.ts` proves the target routes actually pass the required metadata into the shared logging layer.
    - `tests/phase-02/telemetry/health-contract.test.ts` proves the reduced health response shape.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-02/telemetry/telemetry-contract.test.ts tests/phase-02/telemetry/workflow-route-telemetry.test.ts tests/phase-02/telemetry/health-contract.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Operators can inspect actionable structured telemetry for workflow and export behavior, and the health endpoint is hardened to a smaller, safer contract.</done>
</task>

</tasks>

<verification>
Run the telemetry and health contract tests, then run a production build. Inspect the shared log payload builder and the health response to confirm Phase 2 extended the existing JSON logging system rather than introducing a new observability stack.
</verification>

<success_criteria>
SAFE-04 is satisfied when workflow/export routes emit actionable structured JSON telemetry and `/api/health` exposes only the reduced operator-safe contract needed for runtime inspection.
</success_criteria>

<output>
After completion, create `.planning/phases/02-verification-and-telemetry-baseline/02-04-SUMMARY.md`
</output>
