---
phase: 02-verification-and-telemetry-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - tests/e2e/global-setup.ts
  - tests/e2e/generate-smoke.spec.ts
  - scripts/seed-phase-2.mjs
  - app/generate/page.tsx
autonomous: true
requirements:
  - SAFE-03
must_haves:
  truths:
    - "Maintainer can run one browser smoke test that loads a seeded draft into `/generate`, confirms the stage UI hydrates correctly, and reaches review or export readiness without live AI/provider calls."
    - "The SAFE-03 smoke uses persisted seeded data and a real authenticated browser session instead of mocking the page or spending provider quota."
    - "The smoke remains stable across harmless layout changes because selectors are tied to workflow labels or narrow `data-testid` hooks only where roles are ambiguous."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright baseline configured for a seeded local smoke run"
      contains: "Chromium project, local web server settings, and global setup wiring"
    - path: "package.json"
      provides: "Named browser smoke scripts"
      contains: "repo-local install, seed, and smoke scripts for the Phase 2 generate browser check"
    - path: "package-lock.json"
      provides: "Deterministic dependency lockfile updates for Playwright tooling"
      contains: "the resolved graph for Playwright and related browser-test dependencies"
    - path: "tests/e2e/global-setup.ts"
      provides: "Authentication/session bootstrap for seeded local users"
      contains: "login or storage-state setup backed by local Supabase fixtures"
    - path: "tests/e2e/generate-smoke.spec.ts"
      provides: "Executable seeded `/generate` smoke coverage"
      contains: "load, hydrate, navigate, and review/export readiness assertions"
    - path: "scripts/seed-phase-2.mjs"
      provides: "Seeded draft fixture support reused from SAFE-02"
      contains: "a review-ready or export-ready draft scenario for browser smoke"
    - path: "app/generate/page.tsx"
      provides: "Only the minimal stable selectors needed for smoke reliability"
      min_lines: 2000
  key_links:
    - from: "tests/e2e/global-setup.ts"
      to: "scripts/seed-phase-2.mjs"
      via: "shared seeded user credentials and draft ids"
      pattern: "phase-2|seed"
    - from: "tests/e2e/generate-smoke.spec.ts"
      to: "app/generate/page.tsx"
      via: "workflow labels or narrow test ids"
      pattern: "定稿检查|导出|data-testid"
    - from: "tests/e2e/generate-smoke.spec.ts"
      to: "/generate?draft="
      via: "seeded draft load path"
      pattern: "draft="
---

<objective>
Add a seeded Playwright smoke for `/generate` that proves the core workspace can hydrate and reach review/export readiness without live provider calls.

Purpose: SAFE-03 should validate the real browser wiring for the riskiest UI surface while staying deterministic and quota-safe in CI.
Output: Playwright config and scripts, local auth bootstrap, one seeded Chromium smoke spec, and only minimal UI selector hardening where the current page is too ambiguous.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@package.json
@app/generate/page.tsx
@scripts/seed-phase-2.mjs

<interfaces>
From `app/generate/page.tsx`:

```typescript
function StageTabs(props: {
  currentStep: WorkflowStage;
  onChange: (step: WorkflowStage) => void;
  canVisitPlanning: boolean;
  canVisitOutline: boolean;
  canVisitDraft: boolean;
  canVisitReview: boolean;
}) { ... }
```

Representative stable stage labels already present:

```text
1. 信息采集
2. 结构共创
3. 提纲确认
4. 分段成文
5. 定稿检查
```

From research:

```text
SAFE-03 should avoid live AI/provider calls in CI; use seeded drafts/fixtures.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add seeded Playwright runtime wiring on top of the local Supabase fixtures</name>
  <files>package.json, playwright.config.ts, tests/e2e/global-setup.ts, scripts/seed-phase-2.mjs</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
    - package.json
    - scripts/seed-phase-2.mjs
    - app/generate/page.tsx
  </read_first>
  <action>Add Playwright to the repo in the smallest way that supports one deterministic smoke. Create `playwright.config.ts` with a Chromium project, base URL, local web server wiring, and global setup. Prefer a production-ish local server command (`next build` + `next start`) over `next dev` so the smoke is not coupled to the fragile runtime patch path called out in the concerns audit. Extend `scripts/seed-phase-2.mjs` with an e2e scenario that creates a seeded user plus a draft already populated enough to open `/generate?draft=<id>` directly at draft/review readiness. Add `tests/e2e/global-setup.ts` that authenticates the seeded local user and persists storage state for the browser run. Add repo-local setup scripts such as `test:phase-02:e2e:install` and `test:phase-02:e2e:seed` so fresh environments install Chromium explicitly instead of assuming preinstalled browsers.</action>
  <acceptance_criteria>
    - Playwright wiring reuses the Phase 2 seed script rather than inventing a separate data path.
    - The local browser setup does not depend on real AI credentials or external providers.
    - The seeded scenario produces a real draft id and authenticated session the smoke spec can consume deterministically.
    - The package scripts for SAFE-03 are explicit and runnable by maintainers on fresh environments, including browser installation.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:phase-02:rls:reset && npm run test:phase-02:e2e:seed && npm run test:phase-02:e2e:install && npm run build</automated>
  </verify>
  <done>The repo can boot a deterministic, authenticated browser test environment backed by local seeded data instead of live AI traffic.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the seeded generate workflow loads to review/export readiness in Chromium</name>
  <files>tests/e2e/generate-smoke.spec.ts, app/generate/page.tsx, playwright.config.ts, tests/e2e/global-setup.ts, package.json</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-verification-and-telemetry-baseline/02-RESEARCH.md
    - app/generate/page.tsx
    - playwright.config.ts
    - tests/e2e/global-setup.ts
    - scripts/seed-phase-2.mjs
  </read_first>
  <action>Write one browser smoke spec, `tests/e2e/generate-smoke.spec.ts`, that signs in via the global setup state, opens the seeded draft in `/generate`, verifies the workspace hydrates, verifies the expected stage tab is reachable, and proves review/export readiness without triggering live AI flows. Assert on stable workflow labels or accessible roles first. Only add `data-testid` hooks to `app/generate/page.tsx` if roles/text are genuinely ambiguous in this monolithic page. Do not click buttons that trigger live provider calls. Prefer assertions that a seeded review-stage workspace is loaded, version/history UI is visible when expected, and export affordances are enabled. Reuse the repo-local install and seed scripts from Task 1 so the verify path works on CI and fresh laptops.</action>
  <acceptance_criteria>
    - The smoke test covers real browser hydration for `/generate` with a seeded draft id.
    - The smoke does not call live AI routes or depend on paid provider output.
    - Any new `data-testid` hooks are minimal and scoped to ambiguous controls only.
    - The smoke is stable enough to serve as the single SAFE-03 baseline instead of an expanding E2E matrix.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:phase-02:rls:reset && npm run test:phase-02:e2e:seed && npm run test:phase-02:e2e:install && npm run build && npm run test:phase-02:e2e</automated>
  </verify>
  <done>Maintainers have one deterministic browser-level proof that the generate workspace still boots and reaches review/export readiness on seeded real data.</done>
</task>

</tasks>

<verification>
Reset and seed the local Supabase stack, build the app, then run the single Chromium smoke. Confirm the test reaches review/export readiness without any live AI/provider traffic or brittle DOM-structure selectors.
</verification>

<success_criteria>
SAFE-03 is satisfied when maintainers can run one seeded Playwright smoke that exercises the real `/generate` page from draft load through review/export readiness on local data only.
</success_criteria>

<output>
After completion, create `.planning/phases/02-verification-and-telemetry-baseline/02-03-SUMMARY.md`
</output>
