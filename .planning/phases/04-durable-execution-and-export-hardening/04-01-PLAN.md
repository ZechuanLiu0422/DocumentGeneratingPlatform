---
phase: 04-durable-execution-and-export-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - lib/validation.ts
  - lib/operation-store.ts
  - supabase/migrations/20260328110000_phase_04_draft_operations.sql
  - tests/phase-04/contracts/shared-fixtures.ts
  - tests/phase-04/contracts/route-contract-helpers.ts
  - tests/phase-04/contracts/harness-smoke.test.ts
  - tests/phase-04/contracts/operation-foundation-contract.test.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-03
must_haves:
  truths:
    - "Maintainer can run provider-free Phase 4 contract tests that prove durable-operation schemas, operation status transitions, and persisted queue-state normalization before any route is converted to async execution."
    - "The phase introduces a dedicated durable-operation contract instead of overloading `drafts.pending_change`, preserving Phase 3 compare/accept semantics."
    - "Operation persistence is explicit enough for later plans to claim, retry, and complete work idempotently without rewriting the existing workflow layer."
  artifacts:
    - path: "package.json"
      provides: "Named Phase 4 contract commands"
      contains: "repo-local scripts for harness smoke and operation-foundation tests"
    - path: "lib/validation.ts"
      provides: "Shared Phase 4 schemas for durable operations"
      contains: "operation status, operation payload, idempotency-key, and result-shape Zod contracts"
    - path: "lib/operation-store.ts"
      provides: "Route-neutral durable-operation persistence helpers"
      contains: "create, read, normalize, and update helpers for queued/running/succeeded/failed operation rows"
    - path: "supabase/migrations/20260328110000_phase_04_draft_operations.sql"
      provides: "Database storage contract for durable operations"
      contains: "a `draft_operations` table with status, idempotency, lease, attempt, result, and timestamp columns"
    - path: "tests/phase-04/contracts/shared-fixtures.ts"
      provides: "Deterministic Phase 4 draft and operation fixtures"
      contains: "queued/running/succeeded/failed operation rows plus realistic draft/export payload builders"
    - path: "tests/phase-04/contracts/route-contract-helpers.ts"
      provides: "Phase 4 route helper baseline"
      contains: "request builders and temp-module helpers aligned with the existing Phase 2 and Phase 3 contract pattern"
    - path: "tests/phase-04/contracts/harness-smoke.test.ts"
      provides: "Executable proof that the Phase 4 test harness exists before deeper behavior lands"
      contains: "node:test assertions for scripts, migration names, fixture importability, and schema helper presence"
    - path: "tests/phase-04/contracts/operation-foundation-contract.test.ts"
      provides: "Executable proof for durable-operation schemas and normalization"
      contains: "node:test assertions for operation row shaping, idempotency fields, and legal status transitions"
  key_links:
    - from: "tests/phase-04/contracts/operation-foundation-contract.test.ts"
      to: "lib/operation-store.ts"
      via: "operation row normalization and status-transition assertions"
      pattern: "normalize|operation|status|attempt"
    - from: "tests/phase-04/contracts/operation-foundation-contract.test.ts"
      to: "lib/validation.ts"
      via: "schema acceptance and rejection assertions"
      pattern: "operation|safeParse|schema"
    - from: "tests/phase-04/contracts/harness-smoke.test.ts"
      to: "package.json"
      via: "named script existence"
      pattern: "test:phase-04:contracts"
---

<objective>
Create the durable-operation contract foundation before touching request behavior so every later Phase 4 plan can build on stable queue, idempotency, and status semantics.

Purpose: The current risk is hidden coupling between synchronous routes and JSON-heavy draft state. This plan makes durable execution an explicit, testable contract before route adoption work starts.
Output: Phase 4 contract scripts, shared fixtures/helpers, a `draft_operations` schema contract, and route-neutral operation-store helpers.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
@.planning/phases/04-durable-execution-and-export-hardening/04-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@package.json
@lib/validation.ts
@lib/collaborative-store.ts
@lib/api.ts
@tests/phase-03/contracts/shared-fixtures.ts
@tests/phase-03/contracts/trust-contract-helpers.ts

<interfaces>
Recommended durable-operation baseline from research:

```typescript
type DraftOperation = {
  id: string;
  user_id: string;
  draft_id: string | null;
  operation_type: 'draft_generate' | 'draft_regenerate' | 'draft_revise' | 'review' | 'export';
  status: 'queued' | 'running' | 'succeeded' | 'failed' | 'cancelled';
  idempotency_key: string;
  payload: Record<string, unknown>;
  result: Record<string, unknown> | null;
  error_code: string | null;
  error_message: string | null;
  attempt_count: number;
  max_attempts: number;
  lease_token: string | null;
  lease_expires_at: string | null;
  last_heartbeat_at: string | null;
  started_at: string | null;
  completed_at: string | null;
  created_at: string;
  updated_at: string;
};
```

Current comparable brownfield persistence seam:

```typescript
export type DraftRecord = {
  workflow_stage: 'intake' | 'planning' | 'outline' | 'draft' | 'review' | 'done';
  review_state?: ReviewState | null;
  pending_change?: PendingChangeState | null;
  updated_at?: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Phase 4 durable-operation harness and shared fixtures</name>
  <files>package.json, tests/phase-04/contracts/shared-fixtures.ts, tests/phase-04/contracts/route-contract-helpers.ts, tests/phase-04/contracts/harness-smoke.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
    - .planning/phases/04-durable-execution-and-export-hardening/04-VALIDATION.md
    - package.json
    - tests/phase-03/contracts/shared-fixtures.ts
    - tests/phase-03/contracts/trust-contract-helpers.ts
  </read_first>
  <behavior>
    - Test 1: maintainers can invoke named Phase 4 contract scripts on the existing `node:test` runner without adding a second test framework.
    - Test 2: shared fixtures cover draft operation rows, idempotent export requests, and stale-lease cases so later plans avoid brittle inline JSON.
    - Test 3: the helper layer reuses the existing direct-route and temp-module contract style from earlier phases instead of inventing a new harness.
  </behavior>
  <action>Add Phase 4 contract scripts such as `test:phase-04:contracts`, `test:phase-04:contracts:harness`, and a focused `test:phase-04:contracts:foundation` entrypoint in `package.json`. Create `tests/phase-04/contracts/shared-fixtures.ts` with deterministic fixtures for queued/running/succeeded/failed operations, idempotent payloads, and representative draft/export state. Create `tests/phase-04/contracts/route-contract-helpers.ts` by lightly adapting the proven Phase 3 route helper pattern so later async route tests can load Phase 4 routes directly. Add `tests/phase-04/contracts/harness-smoke.test.ts` so Task 1 has executable proof before the deeper schema/persistence assertions from Task 2 exist.</action>
  <acceptance_criteria>
    - `package.json` exposes dedicated Phase 4 contract commands on the existing Node runner.
    - Shared fixtures cover queue state, retry state, and export payloads expected by later plans.
    - The helper layer is generic enough for later route, status, and download contract tests.
    - No live provider, browser, or real database dependency is introduced in this plan.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:phase-04:contracts:harness</automated>
    <automated>rg -n "test:phase-04:contracts" package.json</automated>
  </verify>
  <done>The repo has a reusable Phase 4 contract harness consistent with the current brownfield test style.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define and prove the durable-operation storage contract</name>
  <files>lib/validation.ts, lib/operation-store.ts, supabase/migrations/20260328110000_phase_04_draft_operations.sql, tests/phase-04/contracts/operation-foundation-contract.test.ts, tests/phase-04/contracts/shared-fixtures.ts, tests/phase-04/contracts/route-contract-helpers.ts, package.json</files>
  <read_first>
    - .planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
    - lib/validation.ts
    - lib/collaborative-store.ts
    - lib/api.ts
    - tests/phase-04/contracts/shared-fixtures.ts
    - tests/phase-04/contracts/route-contract-helpers.ts
  </read_first>
  <behavior>
    - Test 1: durable-operation schemas accept legal queue rows and reject missing idempotency, invalid statuses, or malformed result payloads.
    - Test 2: operation-store helpers normalize database rows into stable TypeScript shapes without leaking snake_case/camelCase drift into later route code.
    - Test 3: legal status transitions are explicit: queued -> running -> succeeded/failed/cancelled, with attempt and lease fields preserved.
  </behavior>
  <action>Extend `lib/validation.ts` with Phase 4 Zod contracts for operation types, statuses, idempotency keys, payload/result envelopes, and operation status read models. Add `supabase/migrations/20260328110000_phase_04_draft_operations.sql` to create a `draft_operations` table with explicit status, operation type, idempotency key, attempt count, max attempts, payload/result JSONB, error fields, lease metadata, and timestamps. Create `lib/operation-store.ts` with row normalization and foundational persistence helpers such as create/get/list/update-status operations, but do not add lease-claim or distributed limiter logic yet. Add provider-free node tests that prove these shapes round-trip and that the contract stays separate from `drafts.pending_change`.</action>
  <acceptance_criteria>
    - Durable operations are represented as a dedicated persistence contract, not an extension of `pending_change`.
    - The migration defines the minimum fields later plans need for claiming, retrying, and completing work.
    - Operation-store helpers normalize DB rows into one stable application shape.
    - Foundation tests cover schema rejection and row normalization before route plans build on them.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:phase-04:contracts:foundation</automated>
  </verify>
  <done>Phase 4 has stable, executable durable-operation contracts that later coordination and async route plans can implement against directly.</done>
</task>

</tasks>

<verification>
Run the Phase 4 harness smoke, then the Phase 4 foundation contract script. Confirm the suite proves operation schemas and row normalization without browser startup, live provider calls, or a running Supabase network dependency.
</verification>

<success_criteria>
Phase 4 has a provider-free durable-operation baseline that defines how queued work is represented before any route starts using it.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-execution-and-export-hardening/04-01-SUMMARY.md`
</output>
