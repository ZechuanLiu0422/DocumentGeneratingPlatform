---
phase: 04-durable-execution-and-export-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - package.json
  - lib/document-generator.ts
  - lib/export-artifact-store.ts
  - app/api/generate/route.ts
  - app/api/operations/[id]/download/route.ts
  - app/generate/page.tsx
  - supabase/migrations/20260328113000_phase_04_export_artifacts.sql
  - tests/phase-04/contracts/export-download-contract.test.ts
  - tests/phase-04/ops/export-artifact-contract.test.ts
  - tests/e2e/generate-smoke.spec.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-03
  - UX-03
must_haves:
  truths:
    - "User can start export through the same durable-operation backbone as long-running generation, then retrieve the final document through a server-owned binary download path."
    - "Export completion owns `documents` insert, draft stage transition, version snapshot creation, and artifact metadata exactly once, even when retries occur."
    - "The final export contract no longer returns base64 file bytes in JSON or depends on client-side `atob(...)` decode flows."
  artifacts:
    - path: "lib/export-artifact-store.ts"
      provides: "Durable export artifact metadata and Supabase Storage retrieval helpers"
      contains: "artifact create/get/delete helpers plus `exports/{userId}/{operationId}.docx` path normalization and storage download/upload wiring"
    - path: "supabase/migrations/20260328113000_phase_04_export_artifacts.sql"
      provides: "Database contract for export artifact metadata"
      contains: "an `export_artifacts` table keyed to operations/users/drafts with file name, mime type, byte size, and storage path"
    - path: "app/api/generate/route.ts"
      provides: "Durable export enqueue contract"
      exports:
        - "POST"
    - path: "app/api/operations/[id]/download/route.ts"
      provides: "Binary download path for completed exports"
      exports:
        - "GET"
    - path: "app/generate/page.tsx"
      provides: "Narrow browser download flow updated for operation-based binary export"
      min_lines: 2000
    - path: "tests/phase-04/contracts/export-download-contract.test.ts"
      provides: "Executable binary export contract proof"
      contains: "direct route assertions that completed exports stream/download without `file_data` base64 JSON"
    - path: "tests/phase-04/ops/export-artifact-contract.test.ts"
      provides: "Executable durable artifact lifecycle proof"
      contains: "artifact metadata, idempotent export completion, and retrieval ownership assertions"
    - path: "tests/e2e/generate-smoke.spec.ts"
      provides: "Seeded browser proof for async export readiness and binary download wiring"
      contains: "operation-based export completion and browser download assertions without provider traffic"
  key_links:
    - from: "app/api/generate/route.ts"
      to: "lib/export-artifact-store.ts"
      via: "completed export artifact persistence"
      pattern: "artifact|storage|file_name"
    - from: "app/api/operations/[id]/download/route.ts"
      to: "lib/document-generator.ts"
      via: "binary response path backed by Supabase Storage artifacts produced during export completion"
      pattern: "generateDocumentBuffer|upload|download|Content-Disposition"
    - from: "tests/phase-04/contracts/export-download-contract.test.ts"
      to: "app/api/operations/[id]/download/route.ts"
      via: "direct GET invocation"
      pattern: "GET\\("
    - from: "tests/e2e/generate-smoke.spec.ts"
      to: "app/generate/page.tsx"
      via: "download interaction and completion-state UI"
      pattern: "导出|下载|operation"
---

<objective>
Finish Phase 4 by hardening export onto the durable-operation path and removing the current base64-heavy browser download contract.

Purpose: UX-03 is still missing until export becomes a server-owned binary delivery path, and OPS-01/OPS-03 remain incomplete until export side effects are idempotent and resumable.
Output: Export artifact persistence, durable export enqueue/download routes, minimal client download changes, and seeded browser proof for binary delivery.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
@.planning/phases/04-durable-execution-and-export-hardening/04-03-PLAN.md
@app/api/generate/route.ts
@app/generate/page.tsx
@lib/document-generator.ts
@lib/collaborative-store.ts
@tests/e2e/generate-smoke.spec.ts

<interfaces>
Current export anti-pattern:

```typescript
return ok(context, {
  success: true,
  file_data: fileBuffer.toString('base64'),
  file_name: `${sanitizeTitle(exportPayload.title)}.docx`,
}, 200);
```

Current browser decode path:

```typescript
const byteCharacters = atob(data.file_data);
const blob = new Blob([new Uint8Array(byteNumbers)], { type: DOCX_MIME });
```

Target download contract:

```typescript
GET /api/operations/:id/download
-> 200 binary response with Content-Type DOCX and Content-Disposition attachment
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move export completion and artifact retrieval onto the durable-operation backbone</name>
  <files>package.json, lib/export-artifact-store.ts, lib/document-generator.ts, app/api/generate/route.ts, app/api/operations/[id]/download/route.ts, supabase/migrations/20260328113000_phase_04_export_artifacts.sql, tests/phase-04/contracts/export-download-contract.test.ts, tests/phase-04/ops/export-artifact-contract.test.ts</files>
  <read_first>
    - .planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
    - .planning/phases/04-durable-execution-and-export-hardening/04-03-PLAN.md
    - app/api/generate/route.ts
    - lib/document-generator.ts
    - lib/collaborative-store.ts
  </read_first>
  <behavior>
    - Test 1: export route returns an operation handle instead of base64 document bytes.
    - Test 2: completed export writes the DOCX bytes to Supabase Storage, advances the draft/version state, and persists artifact metadata exactly once through the durable completion path.
    - Test 3: the download route returns a binary DOCX response with `Content-Type` and `Content-Disposition`, and the JSON export contract no longer exposes `file_data`.
  </behavior>
  <action>Add `supabase/migrations/20260328113000_phase_04_export_artifacts.sql` plus `lib/export-artifact-store.ts` for durable export artifact metadata. Use Supabase Storage as the concrete export-byte backend for this phase, with a deterministic object path such as `exports/{userId}/{operationId}.docx`, so completed exports survive request lifetimes and can be re-downloaded safely. Update `app/api/generate/route.ts` so export is enqueued onto the operation backbone rather than synchronously returning a base64 buffer, and explicitly migrate that route to `await enforceDistributedRateLimit(...)` from `lib/distributed-ratelimit.ts` instead of relying on the legacy sync limiter wrapper. Keep `lib/document-generator.ts` as the DOCX builder, but move final binary retrieval behind `app/api/operations/[id]/download/route.ts`, which should validate ownership and stream or return a binary response for completed artifacts backed by Supabase Storage. Ensure export completion owns `documents` insert, draft `done` stage transition, version snapshot creation, review-state carry-forward, and artifact persistence exactly once.</action>
  <acceptance_criteria>
    - Export no longer returns `file_data` base64 JSON in the final contract.
    - Export completion is idempotent and tied to one durable operation record.
    - The binary download route validates user ownership and only serves completed artifacts backed by Supabase Storage object bytes, not regenerated transient output.
    - `app/api/generate/route.ts` explicitly migrates to the async distributed limiter helper for Phase 4 export execution.
    - Tests prove the final contract before any browser proof runs.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-04/contracts/export-download-contract.test.ts tests/phase-04/ops/export-artifact-contract.test.ts</automated>
  </verify>
  <done>Export side effects and artifact retrieval are durable, idempotent, and free of base64 JSON transport.</done>
</task>

<task type="auto">
  <name>Task 2: Update the browser export flow to poll completion and download the final binary artifact</name>
  <files>app/generate/page.tsx, tests/e2e/generate-smoke.spec.ts, package.json</files>
  <read_first>
    - .planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
    - app/generate/page.tsx
    - tests/e2e/generate-smoke.spec.ts
    - app/api/operations/[id]/route.ts
    - app/api/operations/[id]/download/route.ts
  </read_first>
  <action>Update the existing `handleDownload` flow in `app/generate/page.tsx` so it starts an export operation, polls completion through the Phase 4 status route, and triggers the final binary download path once the operation succeeds. Remove the client-side `atob(...)` decode path entirely. Extend the seeded browser smoke so it proves a review-ready local draft can complete the export flow and trigger a binary download without calling live providers. Keep the page changes narrow and aligned with the operation-status UX introduced in `04-03`.</action>
  <acceptance_criteria>
    - The browser no longer decodes base64 bytes from JSON when exporting.
    - Export UX is built on operation polling plus a binary download endpoint.
    - The seeded browser smoke proves export readiness and download wiring on local data only.
    - No broader Phase 5-style workspace decomposition is introduced.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run test:phase-02:rls:reset && npm run test:phase-02:e2e:seed && npm run test:phase-04:e2e</automated>
  </verify>
  <done>User can export through the durable-operation path and receive a normal binary file download without client-side base64 inflation.</done>
</task>

</tasks>

<verification>
Run the export route/artifact contract tests, build the app, then run the seeded browser proof. Confirm export now uses queued completion plus binary retrieval and that the browser flow no longer uses `atob(...)`.
</verification>

<success_criteria>
UX-03 and the export half of OPS-01/OPS-03 are satisfied when users can start export durably, resume after interruption, and download the final DOCX through a server-owned binary path.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-execution-and-export-hardening/04-04-SUMMARY.md`
</output>
