---
phase: 04-durable-execution-and-export-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - package.json
  - lib/distributed-ratelimit.ts
  - lib/ratelimit.ts
  - lib/operation-store.ts
  - lib/api.ts
  - app/api/operations/run/route.ts
  - vercel.json
  - supabase/migrations/20260328111500_phase_04_rate_limit_windows.sql
  - tests/phase-04/ops/distributed-ratelimit.test.ts
  - tests/phase-04/ops/operation-lease-contract.test.ts
  - tests/phase-04/ops/operation-idempotency.test.ts
  - tests/phase-04/ops/runner-drain-contract.test.ts
autonomous: true
requirements:
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "Platform burst limits remain effective across restarts and multiple instances because short-window rate limiting is no longer process-local memory only."
    - "Queued operations can be claimed, heartbeated, released, and retried with lease semantics that prevent duplicate completion."
    - "Later async route plans can depend on one authoritative completion path instead of re-implementing retry and idempotency logic per route."
  artifacts:
    - path: "lib/distributed-ratelimit.ts"
      provides: "Async persistent short-window rate-limit helper"
      contains: "Postgres-backed limit checks keyed by user/action/window and normalized collision handling"
    - path: "supabase/migrations/20260328111500_phase_04_rate_limit_windows.sql"
      provides: "Database contract for distributed short-window limits"
      contains: "a `rate_limit_windows` table or equivalent SQL primitives with unique user/action/window keys and expiry-friendly indexes"
    - path: "lib/ratelimit.ts"
      provides: "Legacy synchronous wrapper plus migration guidance"
      contains: "explicit boundary showing why Phase 4 routes must migrate to an async distributed limiter helper"
    - path: "lib/operation-store.ts"
      provides: "Lease-claim, heartbeat, retry, and completion semantics"
      contains: "claimNextOperation, heartbeatOperation, markOperationSucceeded, markOperationFailed, and stale-lease handling"
    - path: "lib/api.ts"
      provides: "Structured operation telemetry fields"
      contains: "operation_id, attempt_count, lease_token, and operation_status logging metadata"
    - path: "app/api/operations/run/route.ts"
      provides: "Protected runner entrypoint for processing queued work"
      exports:
        - "POST"
    - path: "vercel.json"
      provides: "Autonomous queue-drain trigger"
      contains: "a cron entry or equivalent scheduler configuration that invokes `/api/operations/run` outside the initiating request"
    - path: "tests/phase-04/ops/distributed-ratelimit.test.ts"
      provides: "Executable limiter proof"
      contains: "window collision and reset assertions against the persistent limiter contract"
    - path: "tests/phase-04/ops/operation-lease-contract.test.ts"
      provides: "Executable lease coordination proof"
      contains: "claim, heartbeat, expiry, and stale-lease retry assertions"
    - path: "tests/phase-04/ops/operation-idempotency.test.ts"
      provides: "Executable duplicate-side-effect guard proof"
      contains: "one authoritative completion path even when completion is attempted twice"
    - path: "tests/phase-04/ops/runner-drain-contract.test.ts"
      provides: "Executable proof that queued work reaches a terminal state without a manual user retry"
      contains: "scheduler-triggered drain or self-kick assertions for queued operations"
  key_links:
    - from: "tests/phase-04/ops/distributed-ratelimit.test.ts"
      to: "lib/distributed-ratelimit.ts"
      via: "persistent window-limit assertions"
      pattern: "limit|window|user"
    - from: "tests/phase-04/ops/operation-lease-contract.test.ts"
      to: "lib/operation-store.ts"
      via: "claim/heartbeat/expiry assertions"
      pattern: "claim|lease|heartbeat|expires"
    - from: "tests/phase-04/ops/operation-idempotency.test.ts"
      to: "lib/operation-store.ts"
      via: "duplicate completion assertions"
      pattern: "succeeded|failed|attempt"
---

<objective>
Add the distributed coordination layer Phase 4 needs before any route moves to async operation execution.

Purpose: OPS-02 and OPS-03 fail today because burst limits are process-local and retries have no lease or idempotent completion contract.
Output: Persistent short-window rate limiting, lease-aware operation-store helpers, a protected runner entrypoint, and executable coordination tests.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
@.planning/phases/04-durable-execution-and-export-hardening/04-VALIDATION.md
@.planning/phases/04-durable-execution-and-export-hardening/04-01-PLAN.md
@lib/ratelimit.ts
@lib/quota.ts
@lib/operation-store.ts
@lib/api.ts
@app/api/generate/route.ts
@app/api/ai/draft/route.ts
@app/api/ai/revise/route.ts

<interfaces>
Current process-local limiter:

```typescript
const rateLimitStore = globalThis.__documentRateLimitStore ?? new Map<string, Bucket>();
export function enforceRateLimit(key: string, limit: number, windowMs: number, message: string) { ... }
```

Phase 4 operation foundation from `04-01`:

```typescript
type DraftOperationStatus = 'queued' | 'running' | 'succeeded' | 'failed' | 'cancelled';
createDraftOperation(...)
getDraftOperationById(...)
updateDraftOperation(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace process-local burst limiting with a persistent distributed limiter</name>
  <files>package.json, lib/distributed-ratelimit.ts, lib/ratelimit.ts, supabase/migrations/20260328111500_phase_04_rate_limit_windows.sql, tests/phase-04/ops/distributed-ratelimit.test.ts</files>
  <read_first>
    - .planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
    - lib/ratelimit.ts
    - lib/quota.ts
    - app/api/ai/draft/route.ts
    - app/api/generate/route.ts
  </read_first>
  <behavior>
    - Test 1: short-window limits are enforced through persistent state rather than `globalThis`, so a second process sees the same limit window.
    - Test 2: the new distributed limiter is explicitly async and later Phase 4 route plans know they must `await` it instead of silently relying on the legacy sync contract.
    - Test 3: the persistent limiter resets cleanly when the window expires and reports the same user/action limit semantics as today.
  </behavior>
  <action>Create `supabase/migrations/20260328111500_phase_04_rate_limit_windows.sql` for the persistence primitive the distributed limiter will use, such as a `rate_limit_windows` table with unique `(user_id, action, window_start)` keys and expiry-friendly indexes. Then create `lib/distributed-ratelimit.ts` with an explicitly async Postgres-backed short-window limiter keyed by user, action, and time window. Keep `lib/ratelimit.ts` as a legacy sync wrapper for untouched routes, but do not pretend it transparently upgrades all call sites; document in the helper and tests that the Phase 4 async routes must migrate to `await enforceDistributedRateLimit(...)` in later plans. Add focused tests that prove collision, reset, and multi-instance-safe semantics. Do not remove `enforceDailyQuota(...)`; Phase 4 should layer the persistent burst limiter alongside the existing quota path.</action>
  <acceptance_criteria>
    - The limiter contract is persistent and cross-instance-safe.
    - The persistent limiter has an explicit SQL storage primitive, not just a library abstraction.
    - The plan makes the async migration requirement explicit instead of hiding it behind the old sync helper signature.
    - Tests prove the window behavior without requiring live AI calls.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-04/ops/distributed-ratelimit.test.ts</automated>
  </verify>
  <done>Phase 4 no longer depends on per-process memory for burst limiting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lease-aware claim, heartbeat, retry, and idempotent completion helpers</name>
  <files>package.json, lib/operation-store.ts, lib/api.ts, app/api/operations/run/route.ts, vercel.json, tests/phase-04/ops/operation-lease-contract.test.ts, tests/phase-04/ops/operation-idempotency.test.ts, tests/phase-04/ops/runner-drain-contract.test.ts</files>
  <read_first>
    - .planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
    - .planning/phases/04-durable-execution-and-export-hardening/04-01-PLAN.md
    - lib/operation-store.ts
    - lib/api.ts
    - tests/phase-04/contracts/shared-fixtures.ts
  </read_first>
  <behavior>
    - Test 1: a queued operation can be claimed once, moved to running with a lease token/expiry, and heartbeated while work continues.
    - Test 2: an expired lease can be reclaimed safely without letting two runners finalize the same operation.
    - Test 3: the completion path is idempotent; a second success/failure call for the same operation cannot create a second side effect.
    - Test 4: queued work has an autonomous execution trigger so operations drain to terminal states without a user manually re-posting the route.
  </behavior>
  <action>Extend `lib/operation-store.ts` with explicit lease-aware coordination helpers: claim the next queued operation, heartbeat an in-flight lease, mark success/failure, and release or requeue stale leases. Add a protected runner entrypoint in `app/api/operations/run/route.ts` that can process queued work later without exposing a public worker surface. Make the autonomous execution trigger explicit in this plan: add a scheduler path in `vercel.json` that invokes `/api/operations/run` on a short interval for recovery and stale-lease drainage, and define a best-effort immediate self-kick contract that later enqueue routes will call after creating a queued operation so work starts promptly. Extend `lib/api.ts` so operation lifecycle telemetry can record operation id, attempt count, lease token, and status changes. Add provider-free integration-style node tests that prove claim/expiry/idempotency behavior and that queued work drains to a terminal state through the runner trigger, not a manual user retry.</action>
  <acceptance_criteria>
    - Claim, heartbeat, expiry, and completion semantics are explicit reusable helpers rather than route-local logic.
    - Duplicate completion attempts are rejected or ignored safely through one authoritative path.
    - The plan defines both recovery-time scheduler triggering and immediate post-enqueue triggering for queued work.
    - Operation lifecycle telemetry is ready for later route adoption.
    - Tests prove retry safety and queue drain behavior without requiring the browser or paid providers.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-04/ops/operation-lease-contract.test.ts tests/phase-04/ops/operation-idempotency.test.ts tests/phase-04/ops/runner-drain-contract.test.ts</automated>
  </verify>
  <done>Phase 4 has the coordination primitives required for resilient async execution across restarts and multiple instances.</done>
</task>

</tasks>

<verification>
Run the distributed limiter, lease, and idempotency tests. Confirm Phase 4 now has persistent burst limiting plus one authoritative claim/complete coordination path before routes start enqueueing work.
</verification>

<success_criteria>
OPS-02 and the coordination half of OPS-03 are satisfied when the platform can enforce cross-instance limits and safely claim/retry queued work.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-execution-and-export-hardening/04-02-SUMMARY.md`
</output>
