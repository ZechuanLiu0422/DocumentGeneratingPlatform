---
phase: 04-durable-execution-and-export-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - package.json
  - lib/operation-runner.ts
  - lib/official-document-workflow.ts
  - lib/collaborative-store.ts
  - app/api/ai/draft/route.ts
  - app/api/ai/revise/route.ts
  - app/api/operations/[id]/route.ts
  - app/generate/page.tsx
  - tests/phase-04/contracts/async-draft-route-contract.test.ts
  - tests/phase-04/contracts/async-revise-route-contract.test.ts
  - tests/phase-04/contracts/operation-status-route-contract.test.ts
  - tests/phase-04/telemetry/operation-route-telemetry.test.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-03
must_haves:
  truths:
    - "User can start long-running draft and revise work through an operation handle instead of holding the original request open until provider work completes."
    - "The existing workflow layer is reused by the runner, so async adoption does not fork business logic into a second implementation path."
    - "Operation completion is the only place that mutates authoritative draft/version state for async flows, preventing duplicate side effects on retry."
  artifacts:
    - path: "lib/operation-runner.ts"
      provides: "Dispatch layer from queued operation to existing workflow code"
      contains: "operation-type routing into official-document-workflow and authoritative persistence callbacks"
    - path: "lib/official-document-workflow.ts"
      provides: "Worker-safe workflow entrypoints"
      contains: "reusable draft/regenerate/revise calls that do not assume direct route response ownership"
    - path: "lib/collaborative-store.ts"
      provides: "Authoritative completion helpers for async operations"
      contains: "apply-operation-result helpers that write draft/version state exactly once"
    - path: "app/api/ai/draft/route.ts"
      provides: "Draft-generation enqueue contract"
      exports:
        - "POST"
    - path: "app/api/ai/revise/route.ts"
      provides: "Revise enqueue contract"
      exports:
        - "POST"
    - path: "app/api/operations/[id]/route.ts"
      provides: "Operation status/read-model route"
      exports:
        - "GET"
    - path: "app/generate/page.tsx"
      provides: "Minimal status polling and resume wiring for async work"
      min_lines: 2000
    - path: "tests/phase-04/contracts/async-draft-route-contract.test.ts"
      provides: "Executable draft enqueue contract"
      contains: "operation-handle success responses and normalized error cases"
    - path: "tests/phase-04/contracts/async-revise-route-contract.test.ts"
      provides: "Executable revise enqueue contract"
      contains: "preview-compatible async semantics that preserve Phase 3 compare guarantees"
    - path: "tests/phase-04/contracts/operation-status-route-contract.test.ts"
      provides: "Executable status read-model contract"
      contains: "queued/running/succeeded/failed payload shape assertions"
    - path: "tests/phase-04/telemetry/operation-route-telemetry.test.ts"
      provides: "Executable operation lifecycle telemetry proof"
      contains: "operation_id, attempt_count, and status metadata assertions"
  key_links:
    - from: "app/api/ai/draft/route.ts"
      to: "lib/operation-runner.ts"
      via: "draft enqueue/result application"
      pattern: "operation|enqueue|runner"
    - from: "app/api/ai/revise/route.ts"
      to: "lib/collaborative-store.ts"
      via: "Phase 3 pending-change preservation plus async completion"
      pattern: "pending_change|review_state|saveDraftState"
    - from: "tests/phase-04/contracts/operation-status-route-contract.test.ts"
      to: "app/api/operations/[id]/route.ts"
      via: "direct GET invocation"
      pattern: "GET\\("
    - from: "tests/phase-04/telemetry/operation-route-telemetry.test.ts"
      to: "lib/api.ts"
      via: "shared structured logging assertions"
      pattern: "operation_id|operation_status|attempt"
---

<objective>
Move the longest-running AI workflow actions onto durable operation handles while preserving the current business logic and Phase 3 compare/trust guarantees.

Purpose: OPS-01 and OPS-03 are not satisfied until the browser can start work, poll status, and rely on one authoritative completion path instead of a fragile long-held request.
Output: Runner dispatch helpers, async draft/revise route contracts, operation-status read models, minimal UI polling/resume wiring, and route telemetry proof.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
@.planning/phases/04-durable-execution-and-export-hardening/04-02-PLAN.md
@.planning/phases/03-grounded-drafting-and-review-trust/03-03-PLAN.md
@app/api/ai/draft/route.ts
@app/api/ai/revise/route.ts
@lib/official-document-workflow.ts
@lib/collaborative-store.ts
@lib/generate-workspace.ts
@app/generate/page.tsx
@lib/api.ts

<interfaces>
Current synchronous draft route pattern:

```typescript
enforceRateLimit(`draft:${user.id}`, 10, 60 * 1000, ...);
await enforceDailyQuota(supabase, user.id, 'draft');
const result = await generateDraftWorkflow(...);
await saveDraftState(...);
await createVersionSnapshot(...);
return ok(context, { ... });
```

Phase 3 compare contract to preserve:

```typescript
pending_change: buildPendingChangeState({ candidate, userId, createdAt, baseUpdatedAt })
```

Target async route response shape:

```typescript
{
  mode: 'queued',
  operationId: string,
  status: 'queued' | 'running',
  pollUrl: `/api/operations/${operationId}`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert draft and revise routes to enqueue durable operations</name>
  <files>package.json, lib/operation-runner.ts, lib/official-document-workflow.ts, lib/collaborative-store.ts, app/api/ai/draft/route.ts, app/api/ai/revise/route.ts, tests/phase-04/contracts/async-draft-route-contract.test.ts, tests/phase-04/contracts/async-revise-route-contract.test.ts</files>
  <read_first>
    - .planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
    - .planning/phases/04-durable-execution-and-export-hardening/04-01-PLAN.md
    - .planning/phases/04-durable-execution-and-export-hardening/04-02-PLAN.md
    - app/api/ai/draft/route.ts
    - app/api/ai/revise/route.ts
    - lib/official-document-workflow.ts
    - lib/collaborative-store.ts
  </read_first>
  <behavior>
    - Test 1: draft and revise routes return operation handles for long-running work instead of awaiting provider completion inline.
    - Test 2: the runner reuses existing workflow logic rather than copying prompt/business logic into route handlers.
    - Test 3: authoritative draft/version mutation happens only when the operation runner completes successfully, and Phase 3 `pending_change` semantics stay intact for compare flows.
  </behavior>
  <action>Create `lib/operation-runner.ts` to dispatch queued operation types into the existing `generateDraftWorkflow`, `regenerateSectionWorkflow`, and `reviseWorkflow` helpers, then persist the final result through authoritative completion helpers in `lib/collaborative-store.ts`. Update `app/api/ai/draft/route.ts` and `app/api/ai/revise/route.ts` so long-running provider work is enqueued and returns an operation handle instead of holding the request open. Migrate these touched Phase 4 routes to explicitly `await enforceDistributedRateLimit(...)` from `lib/distributed-ratelimit.ts` rather than pretending the old synchronous helper upgraded itself. Preserve the existing Phase 3 compare-before-accept guarantees: section preview/candidate creation must remain explicit, and the async path must not auto-apply changes that were previously gated by `pending_change` acceptance.</action>
  <acceptance_criteria>
    - Async route contracts return operation handles with a stable poll URL and normalized status fields.
    - The runner reuses existing workflow functions and authoritative persistence helpers.
    - Draft/revise async completion writes draft/version state exactly once.
    - The touched async routes explicitly migrate to the new async distributed limiter helper.
    - Phase 3 compare/accept behavior is preserved; async migration does not silently remove preview gating.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-04/contracts/async-draft-route-contract.test.ts tests/phase-04/contracts/async-revise-route-contract.test.ts</automated>
  </verify>
  <done>Long-running AI work now starts through durable operation handles instead of fragile request-bound execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add operation-status read models, minimal polling UX, and lifecycle telemetry</name>
  <files>app/api/operations/[id]/route.ts, app/generate/page.tsx, lib/api.ts, tests/phase-04/contracts/operation-status-route-contract.test.ts, tests/phase-04/telemetry/operation-route-telemetry.test.ts, package.json</files>
  <read_first>
    - .planning/phases/04-durable-execution-and-export-hardening/04-RESEARCH.md
    - app/generate/page.tsx
    - lib/generate-workspace.ts
    - lib/api.ts
    - tests/e2e/generate-smoke.spec.ts
  </read_first>
  <behavior>
    - Test 1: the operation status route exposes queued/running/succeeded/failed state plus enough result metadata for the client to resume or hydrate completed draft/revise work.
    - Test 2: `/generate` can poll narrow draft/revise status state and recover gracefully after refresh without a Phase 5-scale UI rewrite.
    - Test 3: route telemetry records operation id, attempt count, and lifecycle status transitions through the existing shared logging path.
  </behavior>
  <action>Add `app/api/operations/[id]/route.ts` as the authoritative status/read-model endpoint for queued operations. Update `app/generate/page.tsx` in the smallest possible way so the browser can store current operation ids, poll status, and rehydrate completed draft/revise results without blocking on the initiating request. Extend `lib/api.ts` to log operation lifecycle metadata and add focused route-level telemetry tests. Keep UI work intentionally narrow: this plan should add status and resume wiring only, not begin the Phase 5 workspace decomposition.</action>
  <acceptance_criteria>
    - The browser has one authoritative status route for queued work.
    - The polling UX is narrow and recoverable after refresh.
    - Telemetry extends the shared logging path instead of inventing a second log format.
    - The page changes are limited to operation status and result hydration, not broader layout refactors.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-04/contracts/operation-status-route-contract.test.ts tests/phase-04/telemetry/operation-route-telemetry.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <done>User can start long-running AI work, refresh safely, and observe lifecycle state without holding one fragile request open.</done>
</task>

</tasks>

<verification>
Run the async route-contract, operation-status, and telemetry tests, then build the app. Confirm async route adoption still preserves Phase 3 compare semantics and only adds narrow status/polling UI changes.
</verification>

<success_criteria>
OPS-01 and the draft/revise portion of OPS-03 are satisfied when the browser can start and observe long-running draft and revise work through durable operation handles.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-execution-and-export-hardening/04-03-SUMMARY.md`
</output>
