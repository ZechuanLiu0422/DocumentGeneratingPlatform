---
phase: 01-workflow-integrity-guardrails
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app/generate/page.tsx
  - lib/generate-workspace.ts
  - tests/phase-01/generate-workspace-contract.test.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-03
must_haves:
  truths:
    - "User can save a draft from the generate workspace and keep working from server-confirmed workflow state."
    - "User can reopen an in-progress draft and resume from the last confirmed stage using one hydration path."
    - "User can restore a prior version and have the workspace update from the restored server snapshot instead of manual field patching."
  artifacts:
    - path: "lib/generate-workspace.ts"
      provides: "Pure helpers for editable save payloads and authoritative draft hydration"
      contains: "save payload builder and hydration helpers used by the generate workspace"
    - path: "app/generate/page.tsx"
      provides: "Client save, hydrate, and restore flows aligned to authoritative server responses"
      min_lines: 2000
    - path: "tests/phase-01/generate-workspace-contract.test.ts"
      provides: "Executable client-contract checks for save and hydration flows"
      contains: "node:test coverage for editable payloads and shared hydration"
  key_links:
    - from: "tests/phase-01/generate-workspace-contract.test.ts"
      to: "lib/generate-workspace.ts"
      via: "save payload and hydration assertions"
      pattern: "buildDraftSaveRequest|deriveHydratedDraftView"
    - from: "app/generate/page.tsx"
      to: "lib/generate-workspace.ts"
      via: "shared client contract helpers"
      pattern: "buildDraftSaveRequest|deriveHydratedDraftView"
    - from: "app/generate/page.tsx"
      to: "/api/ai/versions/restore"
      via: "restore fetch"
      pattern: "fetch\\('/api/ai/versions/restore'"
    - from: "app/generate/page.tsx"
      to: "hydrateDraft"
      via: "authoritative response application"
      pattern: "hydrateDraft"
---

<objective>
Align the generate workspace with the new server-owned draft and restore contracts so save/reopen/restore flows all hydrate from authoritative data, and prove the client contract with executable checks.

Purpose: The server boundary from Plans 01 and 02 only becomes user-visible protection when the client stops sending or reconstructing protected workflow state.
Output: Shared client helpers, `app/generate/page.tsx` save and restore handlers that trust server responses instead of local speculative workflow payloads, and a focused contract test.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@app/generate/page.tsx
@lib/generate-workspace.ts
@app/api/drafts/route.ts
@app/api/ai/versions/restore/route.ts
@package.json

<interfaces>
From `app/generate/page.tsx`:

```typescript
const hydrateDraft = (draft: Draft) => {
  setCurrentDraftId(draft.id);
  setWorkflowStage(draft.workflowStage || 'intake');
  setCurrentStep(mapStageToStep(draft.workflowStage || 'intake'));
  ...
};
```

Current save payload:

```typescript
body: JSON.stringify({
  id: currentDraftId,
  docType,
  ...formData,
  provider,
  attachments,
  workflowStage,
  collectedFacts,
  missingFields,
  planning: ...,
  outline: ...,
  sections,
  activeRuleIds,
  activeReferenceIds,
  versionCount: versions.length,
  generatedTitle: formData.title,
  generatedContent: previewContent,
}),
```

Current restore handling:

```typescript
if (data.version?.title) {
  setFormData((prev) => ({ ...prev, title: data.version.title }));
}
setSections(data.version?.sections || []);
setWorkflowStage('draft');
setCurrentStep('draft');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract and test the editable save-payload and hydration contract</name>
  <files>lib/generate-workspace.ts, tests/phase-01/generate-workspace-contract.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - lib/generate-workspace.ts
    - app/generate/page.tsx
    - app/api/drafts/route.ts
    - package.json
  </read_first>
  <behavior>
    - Test 1: saving a draft sends only editable fields to `/api/drafts`.
    - Test 2: a successful save uses the returned authoritative draft snapshot to derive local workflow state rather than keeping local speculative workflow data.
    - Test 3: reopening a draft still lands on the expected stage because the same hydration derivation is used for fetched draft rows and saved draft rows.
  </behavior>
  <action>Extract the generate workspace's save-payload construction and draft-to-view-state hydration derivation into a small pure helper module such as `lib/generate-workspace.ts` so the contract can be exercised through Node built-in tests. The helpers must build an editable-only save request and derive stage/step hydration from the authoritative `draft` snapshot the server returns.</action>
  <acceptance_criteria>
    - `lib/generate-workspace.ts` contains pure helpers for editable save payloads and authoritative draft hydration.
    - The save payload helper does not include `workflowStage`, `collectedFacts`, `missingFields`, `planning`, `outline`, `sections`, `activeRuleIds`, `activeReferenceIds`, `versionCount`, `generatedTitle`, or `generatedContent`.
    - `tests/phase-01/generate-workspace-contract.test.ts` proves the payload and hydration behavior with executable assertions.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-01/generate-workspace-contract.test.ts</automated>
  </verify>
  <done>Manual draft saves no longer compete with server-owned workflow state, and the workspace resumes from the authoritative saved snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse authoritative hydration for version restore and resume flows</name>
  <files>app/generate/page.tsx, lib/generate-workspace.ts, tests/phase-01/generate-workspace-contract.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - app/generate/page.tsx
    - lib/generate-workspace.ts
    - app/api/ai/versions/restore/route.ts
    - app/api/drafts/route.ts
    - tests/phase-01/generate-workspace-contract.test.ts
  </read_first>
  <behavior>
    - Test 1: restoring a version applies the returned restored draft snapshot through the same hydration path used for draft loading.
    - Test 2: workflow stage, form title, outline/planning state, and sections stay synchronized after restore without manual field patching.
  </behavior>
  <action>Update `handleSaveDraft` to send only the editable draft fields accepted by Plan 01 by using the shared payload helper from Task 1. On success, call `hydrateDraft(data.draft)` or a thin wrapper around the shared hydration helper, refresh versions if `draft.id` exists, and keep any local alerts/toasts unchanged. Replace the manual restore-state patching in `handleRestoreVersion` with authoritative hydration from `data.draft` returned by Plan 02. If needed, extract a thin helper around `hydrateDraft` so save, reopen, and restore all use the same server-snapshot application path. Preserve existing version refresh behavior after restore.</action>
  <acceptance_criteria>
    - The `/api/drafts` request body in `app/generate/page.tsx` no longer includes `workflowStage`, `collectedFacts`, `missingFields`, `planning`, `outline`, `sections`, `activeRuleIds`, `activeReferenceIds`, `versionCount`, `generatedTitle`, or `generatedContent`.
    - The save-success branch hydrates from `data.draft`.
    - The current draft id and version refresh logic use the saved draft returned by the server, not a separate locally guessed id.
    - `handleRestoreVersion` hydrates from `data.draft` instead of `data.version`.
    - The restore path no longer manually sets `workflowStage` and `currentStep` from hard-coded values after a successful response.
    - A single hydration path is used for fetched draft rows and restored draft rows.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-01/generate-workspace-contract.test.ts</automated>
    <automated>npm run lint -- --file app/generate/page.tsx --file lib/generate-workspace.ts</automated>
  </verify>
  <done>The generate workspace now resumes and restores from authoritative server snapshots, preserving resumability without browser-owned workflow reconstruction.</done>
</task>

</tasks>

<verification>
Run the generate-workspace contract test, then lint the touched files and inspect the save and restore handlers to confirm both hydrate from server-returned `draft` objects.
</verification>

<success_criteria>
The generate workspace sends only editable draft fields and relies on authoritative server snapshots for save, reopen, and restore flows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-integrity-guardrails/01-03-SUMMARY.md`
</output>
