---
phase: 01-workflow-integrity-guardrails
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/workflow-stage.ts
  - app/api/ai/intake/route.ts
  - app/api/ai/outline-plan/route.ts
  - app/api/ai/outline/route.ts
  - app/api/ai/outline/confirm/route.ts
  - app/api/ai/draft/route.ts
  - app/api/ai/review/route.ts
  - app/api/ai/revise/route.ts
  - app/api/generate/route.ts
  - tests/phase-01/workflow-stage-ownership.test.ts
autonomous: true
requirements:
  - FLOW-02
must_haves:
  truths:
    - "User can advance workflow stage only through supported server workflow actions, not through arbitrary client writes."
    - "Each server workflow route uses an explicit authoritative stage transition instead of open-coded stage strings."
    - "Executable transition checks prove the allowed action-to-stage mappings for intake, planning, outline, drafting, review, and export."
  artifacts:
    - path: "lib/workflow-stage.ts"
      provides: "Centralized authoritative workflow-stage transition helpers"
      contains: "allowed action-to-stage mappings and transition guards"
    - path: "app/api/ai/intake/route.ts"
      provides: "Authoritative intake-stage transition wiring"
      exports:
        - "POST"
    - path: "app/api/ai/outline-plan/route.ts"
      provides: "Authoritative planning-stage transition wiring"
      exports:
        - "POST"
    - path: "app/api/ai/outline/route.ts"
      provides: "Authoritative outline-stage transition wiring"
      exports:
        - "POST"
    - path: "app/api/ai/outline/confirm/route.ts"
      provides: "Authoritative draft-stage transition wiring for confirmed outlines"
      exports:
        - "POST"
    - path: "app/api/ai/draft/route.ts"
      provides: "Authoritative review-stage transition wiring for generated drafts"
      exports:
        - "POST"
    - path: "app/api/ai/review/route.ts"
      provides: "Authoritative review-stage transition wiring for review results"
      exports:
        - "POST"
    - path: "app/api/ai/revise/route.ts"
      provides: "Authoritative review-stage transition wiring for accepted revisions"
      exports:
        - "POST"
    - path: "app/api/generate/route.ts"
      provides: "Authoritative done-stage transition wiring for export completion"
      exports:
        - "POST"
    - path: "tests/phase-01/workflow-stage-ownership.test.ts"
      provides: "Executable workflow-stage ownership checks"
      contains: "node:test coverage for allowed action-to-stage mappings"
  key_links:
    - from: "tests/phase-01/workflow-stage-ownership.test.ts"
      to: "lib/workflow-stage.ts"
      via: "allowed transition assertions"
      pattern: "getAuthoritativeWorkflowStage|assertAuthoritativeWorkflowAction"
    - from: "app/api/ai/intake/route.ts"
      to: "lib/workflow-stage.ts"
      via: "authoritative stage lookup"
      pattern: "getAuthoritativeWorkflowStage|assertAuthoritativeWorkflowAction"
    - from: "app/api/ai/outline-plan/route.ts"
      to: "lib/workflow-stage.ts"
      via: "authoritative stage lookup"
      pattern: "getAuthoritativeWorkflowStage|assertAuthoritativeWorkflowAction"
    - from: "app/api/ai/outline/route.ts"
      to: "lib/workflow-stage.ts"
      via: "authoritative stage lookup"
      pattern: "getAuthoritativeWorkflowStage|assertAuthoritativeWorkflowAction"
    - from: "app/api/ai/outline/confirm/route.ts"
      to: "lib/workflow-stage.ts"
      via: "authoritative stage lookup"
      pattern: "getAuthoritativeWorkflowStage|assertAuthoritativeWorkflowAction"
    - from: "app/api/ai/draft/route.ts"
      to: "lib/workflow-stage.ts"
      via: "authoritative stage lookup"
      pattern: "getAuthoritativeWorkflowStage|assertAuthoritativeWorkflowAction"
    - from: "app/api/ai/review/route.ts"
      to: "lib/workflow-stage.ts"
      via: "authoritative stage lookup"
      pattern: "getAuthoritativeWorkflowStage|assertAuthoritativeWorkflowAction"
    - from: "app/api/ai/revise/route.ts"
      to: "lib/workflow-stage.ts"
      via: "authoritative stage lookup"
      pattern: "getAuthoritativeWorkflowStage|assertAuthoritativeWorkflowAction"
    - from: "app/api/generate/route.ts"
      to: "lib/workflow-stage.ts"
      via: "authoritative stage lookup"
      pattern: "getAuthoritativeWorkflowStage|assertAuthoritativeWorkflowAction"
---

<objective>
Make authoritative workflow-stage transitions explicit across the real server workflow routes so FLOW-02 is fully covered, and prove the allowed mappings with executable checks.

Purpose: The phase goal requires more than blocking `/api/drafts`; stage ownership must live in the supported workflow actions for intake, planning, outline, drafting, review, and export.
Output: A centralized stage-transition helper, route adoption across every legitimate stage-advancing endpoint, and a focused transition-ownership test.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@app/api/ai/intake/route.ts
@app/api/ai/outline-plan/route.ts
@app/api/ai/outline/route.ts
@app/api/ai/outline/confirm/route.ts
@app/api/ai/draft/route.ts
@app/api/ai/review/route.ts
@app/api/ai/revise/route.ts
@app/api/generate/route.ts
@lib/validation.ts
@package.json

<interfaces>
Current server stage writes:

```typescript
// intake
workflow_stage: result.readiness === 'ready' ? 'planning' : 'intake'

// outline planning
workflow_stage: 'planning'

// outline generation
workflow_stage: 'outline'

// outline confirm
workflow_stage: 'draft'

// draft generation
workflow_stage: 'review'

// review apply
workflow_stage: 'review'

// revise apply
workflow_stage: 'review'

// export
workflow_stage: 'done'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize the authoritative workflow-stage map</name>
  <files>lib/workflow-stage.ts, tests/phase-01/workflow-stage-ownership.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - lib/validation.ts
    - app/api/ai/intake/route.ts
    - app/api/ai/outline-plan/route.ts
    - app/api/ai/outline/route.ts
    - app/api/ai/outline/confirm/route.ts
    - app/api/ai/draft/route.ts
    - app/api/ai/review/route.ts
    - app/api/ai/revise/route.ts
    - app/api/generate/route.ts
    - package.json
  </read_first>
  <behavior>
    - Test 1: supported workflow actions resolve to the authoritative target stages the product expects.
    - Test 2: unsupported action names or ad hoc stage requests are rejected by the transition helper.
  </behavior>
  <action>Create a centralized workflow-stage helper that makes the allowed action-to-stage mappings explicit for the legitimate stage-transition routes: intake readiness, outline planning, outline generation, outline confirmation, full draft generation, review application, revision application, and export completion. Back it with a Node built-in test that proves the supported mappings and rejects unsupported actions.</action>
  <acceptance_criteria>
    - `lib/workflow-stage.ts` exposes a small authoritative API such as `getAuthoritativeWorkflowStage(...)` and/or `assertAuthoritativeWorkflowAction(...)`.
    - The helper covers the actual supported transition actions represented by the existing server routes.
    - `tests/phase-01/workflow-stage-ownership.test.ts` proves allowed mappings and unsupported-action rejection.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-01/workflow-stage-ownership.test.ts</automated>
  </verify>
  <done>The allowed workflow-stage transitions are explicit, executable, and no longer implied by scattered string literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adopt authoritative stage transitions across all stage-writing routes</name>
  <files>app/api/ai/intake/route.ts, app/api/ai/outline-plan/route.ts, app/api/ai/outline/route.ts, app/api/ai/outline/confirm/route.ts, app/api/ai/draft/route.ts, app/api/ai/review/route.ts, app/api/ai/revise/route.ts, app/api/generate/route.ts, lib/workflow-stage.ts, tests/phase-01/workflow-stage-ownership.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - lib/workflow-stage.ts
    - app/api/ai/intake/route.ts
    - app/api/ai/outline-plan/route.ts
    - app/api/ai/outline/route.ts
    - app/api/ai/outline/confirm/route.ts
    - app/api/ai/draft/route.ts
    - app/api/ai/review/route.ts
    - app/api/ai/revise/route.ts
    - app/api/generate/route.ts
    - tests/phase-01/workflow-stage-ownership.test.ts
  </read_first>
  <behavior>
    - Test 1: each workflow route that advances stage uses the centralized authoritative transition helper instead of open-coded stage strings.
    - Test 2: the stage ownership checks still pass after route adoption.
  </behavior>
  <action>Refactor every legitimate stage-writing route to obtain its target stage through the centralized helper from Task 1. Keep each route's existing business logic intact, but stop open-coding `workflow_stage` literals in the mutation payloads. This makes the supported workflow actions the only documented and implemented path for moving through intake, planning, outline, drafting, review, and export.</action>
  <acceptance_criteria>
    - `app/api/ai/intake/route.ts`, `app/api/ai/outline-plan/route.ts`, `app/api/ai/outline/route.ts`, `app/api/ai/outline/confirm/route.ts`, `app/api/ai/draft/route.ts`, `app/api/ai/review/route.ts`, `app/api/ai/revise/route.ts`, and `app/api/generate/route.ts` use the centralized stage helper.
    - No route in the supported transition set relies on an ad hoc `workflow_stage: '...'` literal after the refactor, except where an intake helper still maps readiness through the shared transition API.
    - The phase now has explicit plan coverage for the real server-side transition paths required by FLOW-02.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-01/workflow-stage-ownership.test.ts</automated>
    <automated>npm run lint -- --file lib/workflow-stage.ts --file app/api/ai/intake/route.ts --file app/api/ai/outline-plan/route.ts --file app/api/ai/outline/route.ts --file app/api/ai/outline/confirm/route.ts --file app/api/ai/draft/route.ts --file app/api/ai/review/route.ts --file app/api/ai/revise/route.ts --file app/api/generate/route.ts</automated>
  </verify>
  <done>The supported server workflow actions now own stage transitions explicitly, satisfying FLOW-02 beyond the `/api/drafts` hardening work.</done>
</task>

</tasks>

<verification>
Run the workflow-stage ownership test, then lint the touched files and inspect the stage-writing routes to confirm they use the centralized authoritative transition helper.
</verification>

<success_criteria>
The only supported path for advancing workflow stage is through the explicit server workflow actions, and the allowed action-to-stage mappings are centrally defined and executable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-integrity-guardrails/01-04-SUMMARY.md`
</output>
