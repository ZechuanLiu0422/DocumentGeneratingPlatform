---
phase: 01-workflow-integrity-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/collaborative-store.ts
  - app/api/ai/versions/restore/route.ts
autonomous: true
requirements:
  - FLOW-03
  - FLOW-04
must_haves:
  truths:
    - "User can restore a prior version and continue from a coherent draft state without losing current workflow metadata."
    - "Version restore returns the same authoritative draft shape used by draft loading, so resume behavior is deterministic."
    - "Server-side version counting remains authoritative during restore operations."
  artifacts:
    - path: "lib/collaborative-store.ts"
      provides: "Restore helper that writes and re-reads the authoritative draft snapshot"
      contains: "restoreVersionSnapshot"
    - path: "app/api/ai/versions/restore/route.ts"
      provides: "Restore endpoint returning the refreshed draft snapshot"
      exports:
        - "POST"
  key_links:
    - from: "app/api/ai/versions/restore/route.ts"
      to: "lib/collaborative-store.ts"
      via: "restoreVersionSnapshot"
      pattern: "restoreVersionSnapshot"
    - from: "lib/collaborative-store.ts"
      to: "document_versions"
      via: "restore snapshot insert/read"
      pattern: "from\\('document_versions'\\)"
    - from: "lib/collaborative-store.ts"
      to: "drafts"
      via: "authoritative restore write"
      pattern: "from\\('drafts'\\)\\.update"
---

<objective>
Harden version restore and version-count persistence so restored drafts resume from a coherent server-owned snapshot.

Purpose: FLOW-03 and FLOW-04 depend on restore behavior being deterministic and on version metadata staying server-owned.
Output: A restore helper and route that return the refreshed draft snapshot after restore instead of leaving the UI to reconstruct state ad hoc.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@lib/collaborative-store.ts
@app/api/ai/versions/restore/route.ts
@app/api/ai/versions/route.ts
@app/api/ai/outline/confirm/route.ts

<interfaces>
From `lib/collaborative-store.ts`:

```typescript
export async function createVersionSnapshot(supabase, payload) { ... }
export async function restoreVersionSnapshot(supabase, userId, draftId, versionId) { ... }
export async function getDraftById(supabase, userId, draftId) { ... }
```

Current restore behavior:

```typescript
await supabase.from('drafts').update({
  generated_title: data.title || null,
  generated_content: data.content || null,
  sections: normalizedSections,
  workflow_stage: 'draft',
  updated_at: new Date().toISOString(),
});

return {
  ...data,
  sections: normalizedSections,
} as VersionRecord;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make restore write and return an authoritative draft snapshot</name>
  <files>lib/collaborative-store.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - lib/collaborative-store.ts
    - app/api/ai/versions/restore/route.ts
    - app/api/ai/outline/confirm/route.ts
  </read_first>
  <behavior>
    - Test 1: restoring a saved draft/review/export version updates generated title/content/sections and leaves planning, outline, active rules, active references, and base fields coherent.
    - Test 2: the restore helper returns the refreshed `DraftRecord` shape from the `drafts` table instead of a raw `VersionRecord`.
    - Test 3: restore still appends a `restored` snapshot so version history remains auditable and `version_count` stays server-derived.
  </behavior>
  <action>Refactor `restoreVersionSnapshot` so it treats restore as an authoritative server mutation: read the current draft, apply the selected snapshot's generated title/content/sections onto that draft, preserve current planning/outline/base metadata unless the selected snapshot explicitly changes them, write the updated row, create the `restored` version snapshot, and then re-read the draft through `getDraftById` or the shared normalizer before returning. Do not return a raw `VersionRecord` from the helper after the mutation.</action>
  <acceptance_criteria>
    - `restoreVersionSnapshot` returns a normalized draft snapshot rather than `VersionRecord`.
    - The restore update path preserves existing `planning`, `outline`, `active_rule_ids`, `active_reference_ids`, and base draft metadata while replacing generated content fields from the selected version.
    - The helper still creates a `restored` version snapshot after the write so history remains append-only.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint -- --file lib/collaborative-store.ts --file app/api/ai/versions/restore/route.ts</automated>
  </verify>
  <done>Restore no longer leaves the UI guessing how to merge a raw version row back into draft state; the server returns the refreshed authoritative draft.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return restored draft state from the version restore route</name>
  <files>app/api/ai/versions/restore/route.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - app/api/ai/versions/restore/route.ts
    - lib/collaborative-store.ts
    - app/generate/page.tsx
  </read_first>
  <behavior>
    - Test 1: `POST /api/ai/versions/restore` responds with the refreshed draft snapshot that the UI can hydrate directly.
    - Test 2: the route contract is explicit that restore returns authoritative draft state, not only the selected version metadata.
  </behavior>
  <action>Update the restore route to return the authoritative restored draft payload from Task 1, plus only the minimum restore metadata needed for messaging if useful. Keep auth and validation unchanged. The route response should line up with `hydrateDraft` expectations in `app/generate/page.tsx` so the UI can resume from the last confirmed stage without bespoke patch logic per FLOW-03 and FLOW-04.</action>
  <acceptance_criteria>
    - The success payload in `app/api/ai/versions/restore/route.ts` includes `draft`.
    - The route no longer exposes the raw restore result as `version` only.
    - The route still validates `draftId` and `versionId` with `versionRestoreSchema`.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint -- --file app/api/ai/versions/restore/route.ts --file lib/collaborative-store.ts</automated>
  </verify>
  <done>The restore endpoint now provides a single authoritative source for resumed draft state, enabling deterministic reopen and continue-editing behavior.</done>
</task>

</tasks>

<verification>
Run the targeted lint command, then inspect the restore response contract and helper return type to confirm the UI can consume a `draft` snapshot directly.
</verification>

<success_criteria>
Restoring a version yields an authoritative draft snapshot with coherent generated content, preserved workflow metadata, and server-derived version history.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-integrity-guardrails/01-02-SUMMARY.md`
</output>
