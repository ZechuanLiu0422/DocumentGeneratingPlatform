---
phase: 01-workflow-integrity-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/collaborative-store.ts
  - lib/version-restore.ts
  - app/api/ai/versions/restore/route.ts
  - tests/phase-01/version-restore-contract.test.ts
autonomous: true
requirements:
  - FLOW-03
  - FLOW-04
must_haves:
  truths:
    - "User can restore a prior version and continue from a coherent draft state without losing current workflow metadata."
    - "Version restore returns the same authoritative draft shape used by draft loading, so resume behavior is deterministic."
    - "Server-side version counting remains authoritative during restore operations."
  artifacts:
    - path: "lib/version-restore.ts"
      provides: "Pure restore merge helpers that preserve authoritative draft metadata"
      contains: "restore merge and response-shape helpers"
    - path: "lib/collaborative-store.ts"
      provides: "Restore helper that writes and re-reads the authoritative draft snapshot"
      contains: "restoreVersionSnapshot"
    - path: "app/api/ai/versions/restore/route.ts"
      provides: "Restore endpoint returning the refreshed draft snapshot"
      exports:
        - "POST"
    - path: "tests/phase-01/version-restore-contract.test.ts"
      provides: "Executable restore-contract checks"
      contains: "node:test coverage for preserved workflow metadata and authoritative draft returns"
  key_links:
    - from: "tests/phase-01/version-restore-contract.test.ts"
      to: "lib/version-restore.ts"
      via: "restore merge contract assertions"
      pattern: "mergeRestoredDraft|buildRestoreResponse"
    - from: "app/api/ai/versions/restore/route.ts"
      to: "lib/version-restore.ts"
      via: "authoritative restore shaping"
      pattern: "mergeRestoredDraft|buildRestoreResponse"
    - from: "lib/collaborative-store.ts"
      to: "document_versions"
      via: "restore snapshot insert/read"
      pattern: "from\\('document_versions'\\)"
    - from: "lib/collaborative-store.ts"
      to: "drafts"
      via: "authoritative restore write"
      pattern: "from\\('drafts'\\)\\.update"
---

<objective>
Harden version restore and version-count persistence so restored drafts resume from a coherent server-owned snapshot, and prove the merge contract with executable checks.

Purpose: FLOW-03 and FLOW-04 depend on restore behavior being deterministic and on version metadata staying server-owned.
Output: Restore merge helpers, a restore helper and route that return the refreshed draft snapshot after restore, and a focused contract test that proves authoritative metadata is preserved.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@lib/collaborative-store.ts
@lib/version-restore.ts
@app/api/ai/versions/restore/route.ts
@app/api/ai/versions/route.ts
@app/api/ai/outline/confirm/route.ts
@package.json

<interfaces>
From `lib/collaborative-store.ts`:

```typescript
export async function createVersionSnapshot(supabase, payload) { ... }
export async function restoreVersionSnapshot(supabase, userId, draftId, versionId) { ... }
export async function getDraftById(supabase, userId, draftId) { ... }
```

Current restore behavior:

```typescript
await supabase.from('drafts').update({
  generated_title: data.title || null,
  generated_content: data.content || null,
  sections: normalizedSections,
  workflow_stage: 'draft',
  updated_at: new Date().toISOString(),
});

return {
  ...data,
  sections: normalizedSections,
} as VersionRecord;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract and test the authoritative restore-merge contract</name>
  <files>lib/version-restore.ts, tests/phase-01/version-restore-contract.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - lib/version-restore.ts
    - lib/collaborative-store.ts
    - app/api/ai/versions/restore/route.ts
    - app/api/ai/outline/confirm/route.ts
    - package.json
  </read_first>
  <behavior>
    - Test 1: restoring a saved draft/review/export version updates generated title/content/sections and leaves planning, outline, active rules, active references, and base fields coherent.
    - Test 2: the restore merge helper returns a `DraftRecord`-shaped result instead of a raw `VersionRecord`.
    - Test 3: restore still appends a `restored` snapshot so version history remains auditable and `version_count` stays server-derived.
  </behavior>
  <action>Extract the pure restore merge and response-shaping logic into `lib/version-restore.ts` so it can be executed through Node built-in tests. The helper should model restore as an authoritative server mutation: selected snapshot content replaces generated title/content/sections, current planning/outline/base metadata remain intact unless explicitly changed by the workflow, and the resulting object stays aligned to the normalized draft shape the UI hydrates.</action>
  <acceptance_criteria>
    - `lib/version-restore.ts` exposes pure restore merge helpers usable from tests and the route/store code.
    - The restore merge path preserves existing `planning`, `outline`, `active_rule_ids`, `active_reference_ids`, and base draft metadata while replacing generated content fields from the selected version.
    - `tests/phase-01/version-restore-contract.test.ts` proves the merge behavior and authoritative draft return shape.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-01/version-restore-contract.test.ts</automated>
  </verify>
  <done>Restore no longer leaves the UI guessing how to merge a raw version row back into draft state; the server returns the refreshed authoritative draft.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return restored draft state from the version restore route</name>
  <files>lib/collaborative-store.ts, app/api/ai/versions/restore/route.ts, lib/version-restore.ts, tests/phase-01/version-restore-contract.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - app/api/ai/versions/restore/route.ts
    - lib/collaborative-store.ts
    - lib/version-restore.ts
    - app/generate/page.tsx
    - tests/phase-01/version-restore-contract.test.ts
  </read_first>
  <behavior>
    - Test 1: `POST /api/ai/versions/restore` responds with the refreshed draft snapshot that the UI can hydrate directly.
    - Test 2: the route contract is explicit that restore returns authoritative draft state, not only the selected version metadata.
  </behavior>
  <action>Refactor `restoreVersionSnapshot` so it uses the pure merge helpers from Task 1, reads the current draft, applies the selected snapshot's generated content onto it, writes the updated row, creates the `restored` version snapshot, and re-reads the draft through `getDraftById` or the shared normalizer before returning. Update the restore route to return the authoritative restored draft payload, plus only the minimum restore metadata needed for messaging if useful. Keep auth and validation unchanged. The route response should line up with `hydrateDraft` expectations in `app/generate/page.tsx` so the UI can resume from the last confirmed stage without bespoke patch logic per FLOW-03 and FLOW-04.</action>
  <acceptance_criteria>
    - `restoreVersionSnapshot` returns a normalized draft snapshot rather than `VersionRecord`.
    - The helper still creates a `restored` version snapshot after the write so history remains append-only.
    - The success payload in `app/api/ai/versions/restore/route.ts` includes `draft`.
    - The route no longer exposes the raw restore result as `version` only.
    - The route still validates `draftId` and `versionId` with `versionRestoreSchema`.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-01/version-restore-contract.test.ts</automated>
    <automated>npm run lint -- --file app/api/ai/versions/restore/route.ts --file lib/collaborative-store.ts --file lib/version-restore.ts</automated>
  </verify>
  <done>The restore endpoint now provides a single authoritative source for resumed draft state, enabling deterministic reopen and continue-editing behavior.</done>
</task>

</tasks>

<verification>
Run the restore contract test, then lint the touched files and inspect the restore response contract and helper return type to confirm the UI can consume a `draft` snapshot directly.
</verification>

<success_criteria>
Restoring a version yields an authoritative draft snapshot with coherent generated content, preserved workflow metadata, and server-derived version history.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-integrity-guardrails/01-02-SUMMARY.md`
</output>
