---
phase: 03-grounded-drafting-and-review-trust
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - package.json
  - lib/official-document-workflow.ts
  - lib/collaborative-store.ts
  - app/api/ai/review/route.ts
  - app/api/generate/route.ts
  - app/generate/page.tsx
  - supabase/migrations/20260327173000_phase_03_review_state_jsonb.sql
  - tests/phase-03/contracts/review-policy-contract.test.ts
  - tests/phase-03/contracts/export-review-gate-contract.test.ts
  - tests/phase-03/telemetry/trust-route-telemetry.test.ts
autonomous: true
requirements:
  - TRUST-03
must_haves:
  truths:
    - "User can run deterministic document-type-specific checks before export and see actionable pass/warning/fail results."
    - "The latest review result is tied to a persisted content hash so export can reject stale or missing reviews instead of silently exporting unchecked content."
    - "Review and export trust behavior remains observable through the existing Phase 2 telemetry path."
  artifacts:
    - path: "lib/official-document-workflow.ts"
      provides: "Deterministic policy-pack review plus review-hash shaping"
      contains: "doc-type-specific checks for notice/letter/request/report before AI review augmentation"
    - path: "lib/collaborative-store.ts"
      provides: "Persistence for `drafts.review_state jsonb` and `document_versions.review_state jsonb`"
      contains: "hash, status, ranAt, and checks needed to gate export freshness and snapshot accepted review state"
    - path: "app/api/ai/review/route.ts"
      provides: "Review route that persists freshness-aware review state"
      exports:
        - "POST"
    - path: "app/api/generate/route.ts"
      provides: "Export preflight gate tied to current review freshness"
      exports:
        - "POST"
    - path: "app/generate/page.tsx"
      provides: "Minimal stale-review or review-required messaging in the existing workspace"
      min_lines: 2000
    - path: "tests/phase-03/contracts/review-policy-contract.test.ts"
      provides: "Executable review-policy coverage"
      contains: "doc-type-specific deterministic check assertions and review-hash persistence expectations"
    - path: "tests/phase-03/contracts/export-review-gate-contract.test.ts"
      provides: "Executable export gate coverage"
      contains: "direct route assertions for REVIEW_REQUIRED and REVIEW_STALE contracts"
    - path: "tests/phase-03/telemetry/trust-route-telemetry.test.ts"
      provides: "Executable telemetry adoption checks for trust-specific fields"
      contains: "route-level assertions for review_hash, review_status, and export-gate metadata"
  key_links:
    - from: "app/api/ai/review/route.ts"
      to: "lib/official-document-workflow.ts"
      via: "review workflow call"
      pattern: "reviewWorkflow"
    - from: "app/api/generate/route.ts"
      to: "lib/collaborative-store.ts"
      via: "authoritative `drafts.review_state` lookup before export"
      pattern: "getDraftById|review_state"
    - from: "tests/phase-03/contracts/export-review-gate-contract.test.ts"
      to: "app/api/generate/route.ts"
      via: "direct POST invocation"
      pattern: "POST\\("
    - from: "tests/phase-03/telemetry/trust-route-telemetry.test.ts"
      to: "lib/api.ts"
      via: "shared structured logging assertions"
      pattern: "review_hash|review_status"
---

<objective>
Make review results deterministic, document-specific, and freshness-aware so export trust is enforced instead of implied.

Purpose: TRUST-03 fails today because review is generic and export ignores whether the checks still match the current content.
Output: Doc-type policy packs, persisted review state with content hash, export preflight gating, and trust-route telemetry/assertions.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-grounded-drafting-and-review-trust/03-RESEARCH.md
@.planning/phases/03-grounded-drafting-and-review-trust/03-01-PLAN.md
@.planning/phases/02-verification-and-telemetry-baseline/02-04-SUMMARY.md
@lib/official-document-workflow.ts
@lib/collaborative-store.ts
@app/api/ai/review/route.ts
@app/api/generate/route.ts
@app/generate/page.tsx
@lib/api.ts

<interfaces>
From `app/api/ai/review/route.ts`:

```typescript
const checks = await reviewWorkflow({
  docType: draft.doc_type,
  provider: body.provider,
  title: draft.generated_title || draft.title || '',
  recipient: draft.recipient || '',
  facts: draft.collected_facts,
  content: draft.generated_content || '',
  sections: draft.sections,
  rules,
  references,
  attachments: draft.attachments,
});
```

From `app/api/generate/route.ts`:

```typescript
const draft = body.draftId ? await getDraftById(supabase, user.id, body.draftId) : null;
const fileBuffer = await generateDocumentBuffer(body.docType, { ... });
```

Chosen review-state storage contract:

```text
drafts.review_state jsonb = authoritative latest review result for export freshness
document_versions.review_state jsonb = accepted review snapshot stored with accepted version history
```

From Phase 2 telemetry:

```text
workflow/export metadata should extend the existing `lib/api.ts` JSON logging path, not create a new log format.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic document-type policy packs and persist review freshness state</name>
  <files>package.json, lib/official-document-workflow.ts, lib/collaborative-store.ts, app/api/ai/review/route.ts, supabase/migrations/20260327173000_phase_03_review_state_jsonb.sql, tests/phase-03/contracts/review-policy-contract.test.ts</files>
  <read_first>
    - .planning/phases/03-grounded-drafting-and-review-trust/03-RESEARCH.md
    - lib/official-document-workflow.ts
    - lib/collaborative-store.ts
    - app/api/ai/review/route.ts
    - lib/validation.ts
  </read_first>
  <behavior>
    - Test 1: `notice`, `letter`, `request`, and `report` each run deterministic checks specific to their required structure/endings before any AI augmentation.
    - Test 2: review results persist a content hash and status tied to the current accepted title/sections/content.
    - Test 3: rerunning review after content changes produces a new review hash instead of silently reusing stale checks.
  </behavior>
  <action>Expand `reviewWorkflow(...)` with explicit deterministic policy packs for each supported document type, using the research baseline: notice action/work requirement, letter cooperation framing, request approval focus, and report progress/issues/next-steps coverage. Keep AI review as an augmenting layer after deterministic checks, not a replacement. Consume the concrete storage contract from `03-01`: persist the latest review result into `drafts.review_state jsonb` and write the accepted review snapshot into `document_versions.review_state jsonb` whenever an accepted review snapshot is created. Update `app/api/ai/review/route.ts` and `lib/collaborative-store.ts` to save and return that exact JSONB contract, and keep `supabase/migrations/20260327173000_phase_03_review_state_jsonb.sql` in scope for any schema adjustments discovered during execution. Add node tests that assert policy-pack coverage and review-hash persistence without calling providers.</action>
  <acceptance_criteria>
    - Deterministic doc-type checks run before AI review.
    - `drafts.review_state jsonb` is the authoritative export-gating record and `document_versions.review_state jsonb` is the accepted review snapshot history.
    - The route response includes freshness-relevant metadata the client can use.
    - Provider-free tests cover both policy rules and content-hash changes.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-03/contracts/review-policy-contract.test.ts</automated>
  </verify>
  <done>Review is now a freshness-aware trust check with deterministic policy coverage for each supported document type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate export on current review freshness and surface trust-state failures clearly</name>
  <files>app/api/generate/route.ts, app/generate/page.tsx, tests/phase-03/contracts/export-review-gate-contract.test.ts, tests/phase-03/telemetry/trust-route-telemetry.test.ts, lib/api.ts, package.json</files>
  <read_first>
    - .planning/phases/03-grounded-drafting-and-review-trust/03-RESEARCH.md
    - .planning/phases/02-verification-and-telemetry-baseline/02-04-SUMMARY.md
    - app/api/generate/route.ts
    - app/generate/page.tsx
    - lib/api.ts
  </read_first>
  <behavior>
    - Test 1: export is rejected with a normalized `REVIEW_REQUIRED` or `REVIEW_STALE` contract when no current review result matches the draft content.
    - Test 2: `/generate` shows a narrow stale-review/review-required warning in the existing review/export area instead of silently failing.
    - Test 3: review/export trust telemetry includes `review_hash` and `review_status` so operators can diagnose trust-gate failures.
  </behavior>
  <action>Update `app/api/generate/route.ts` so export first reads `drafts.review_state jsonb` from the authoritative draft record and verifies its `content_hash` matches the current accepted title/sections/content; if not, return a normalized `REVIEW_REQUIRED` or `REVIEW_STALE` trust-gate error and keep the workflow in review. On accepted export snapshots, ensure the saved `document_versions.review_state jsonb` remains aligned with the exported accepted content. Add minimal review-status messaging and retry guidance to the review/export area of `app/generate/page.tsx` without decomposing the page. Extend the existing `lib/api.ts` telemetry metadata and add a focused route-level telemetry test for review hash/status on review/export routes. Keep verification provider-free: export gate tests should stub document generation and assert the trust gate before any expensive work starts.</action>
  <acceptance_criteria>
    - Export refuses stale or missing review state before generating the Word buffer.
    - Export uses `drafts.review_state jsonb` as its only freshness source; it does not infer freshness from client state.
    - The review/export UI communicates why export is blocked and how to recover.
    - Trust-route telemetry extends the existing shared logging path.
    - No paid-provider call is required to prove the review gate works.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-03/contracts/export-review-gate-contract.test.ts tests/phase-03/telemetry/trust-route-telemetry.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <done>User can only export content that has a current document-specific review result, and operators can see trust-gate failures in existing telemetry.</done>
</task>

</tasks>

<verification>
Run the review-policy, export-gate, and trust-telemetry contract tests, then build the app. Confirm export gating fires before document generation and that the workspace only gets narrow review-state messaging additions.
</verification>

<success_criteria>
TRUST-03 is satisfied when review results are document-specific, persisted with a content hash, and enforced as a prerequisite for export.
</success_criteria>

<output>
After completion, create `.planning/phases/03-grounded-drafting-and-review-trust/03-03-SUMMARY.md`
</output>
