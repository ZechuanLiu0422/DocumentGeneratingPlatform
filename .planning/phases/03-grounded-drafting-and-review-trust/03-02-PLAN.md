---
phase: 03-grounded-drafting-and-review-trust
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/official-document-workflow.ts
  - app/api/ai/draft/route.ts
  - app/api/ai/versions/route.ts
  - app/generate/page.tsx
  - tests/phase-03/contracts/grounded-draft-route-contract.test.ts
  - tests/phase-03/contracts/provenance-read-model-contract.test.ts
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
must_haves:
  truths:
    - "User can see that accepted generated sections cite a narrow set of approved sources instead of generic reference summaries only."
    - "User can inspect provenance for each accepted section in the existing generate workspace without a broad UI decomposition."
    - "Version and draft read models expose stable provenance data so review history and future compare flows use the same accepted source map."
  artifacts:
    - path: "lib/official-document-workflow.ts"
      provides: "Grounded excerpt selection plus section-level provenance generation"
      contains: "reference selection logic that chooses a small accepted evidence set per section"
    - path: "app/api/ai/draft/route.ts"
      provides: "Draft-generation route contracts that return accepted sections with provenance metadata"
      exports:
        - "POST"
    - path: "app/api/ai/versions/route.ts"
      provides: "Version read model enriched for provenance-aware review surfaces"
      exports:
        - "GET"
    - path: "app/generate/page.tsx"
      provides: "Minimal provenance inspection UI inside the existing draft/review panes"
      min_lines: 2000
    - path: "tests/phase-03/contracts/grounded-draft-route-contract.test.ts"
      provides: "Executable route coverage for grounded draft generation"
      contains: "direct route assertions for provenance-bearing full and section draft responses"
    - path: "tests/phase-03/contracts/provenance-read-model-contract.test.ts"
      provides: "Executable read-model coverage for provenance inspection"
      contains: "node:test assertions for version listing and UI-facing provenance shaping"
  key_links:
    - from: "app/api/ai/draft/route.ts"
      to: "lib/official-document-workflow.ts"
      via: "grounded draft workflow call"
      pattern: "generateDraftWorkflow|regenerateSectionWorkflow"
    - from: "app/generate/page.tsx"
      to: "app/api/ai/versions/route.ts"
      via: "version/provenance read model fetch"
      pattern: "/api/ai/versions"
    - from: "tests/phase-03/contracts/grounded-draft-route-contract.test.ts"
      to: "app/api/ai/draft/route.ts"
      via: "direct POST invocation"
      pattern: "POST\\("
    - from: "tests/phase-03/contracts/provenance-read-model-contract.test.ts"
      to: "app/api/ai/versions/route.ts"
      via: "GET response-shape assertions"
      pattern: "GET\\("
---

<objective>
Implement accepted-content provenance for drafting and expose it through narrow review UI additions, without decomposing the monolithic `/generate` page.

Purpose: TRUST-01 and TRUST-02 require inspectable grounding, but research explicitly warns against broad UI decomposition in this phase.
Output: Grounded excerpt selection in the workflow layer, provenance-bearing draft/version contracts, and minimal provenance inspection UI in the existing generate workspace.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-grounded-drafting-and-review-trust/03-RESEARCH.md
@.planning/phases/03-grounded-drafting-and-review-trust/03-01-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@app/generate/page.tsx
@app/api/ai/draft/route.ts
@app/api/ai/versions/route.ts
@lib/official-document-workflow.ts
@lib/collaborative-store.ts
@lib/validation.ts
@tests/e2e/generate-smoke.spec.ts

<interfaces>
From `lib/official-document-workflow.ts`:

```typescript
export async function regenerateSectionWorkflow(params: {
  docType: DocType;
  provider: Provider;
  facts: Record<string, string | string[]>;
  title: string;
  outlineSections: OutlineSectionRecord[];
  currentSections: DraftSectionRecord[];
  targetSectionId: string;
  rules?: WritingRuleRecord[];
  references?: ReferenceAssetRecord[];
  attachments?: string[];
})
```

From `app/api/ai/draft/route.ts`:

```typescript
return ok(context, {
  title: result.title,
  content: result.content,
  sections: result.sections,
});
```

From `app/generate/page.tsx`:

```typescript
type DraftSection = {
  id: string;
  heading: string;
  body: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ground full and section draft generation in approved excerpt evidence</name>
  <files>lib/official-document-workflow.ts, app/api/ai/draft/route.ts, tests/phase-03/contracts/grounded-draft-route-contract.test.ts</files>
  <read_first>
    - .planning/phases/03-grounded-drafting-and-review-trust/03-RESEARCH.md
    - .planning/phases/03-grounded-drafting-and-review-trust/03-01-PLAN.md
    - lib/official-document-workflow.ts
    - app/api/ai/draft/route.ts
    - lib/collaborative-store.ts
    - lib/validation.ts
  </read_first>
  <behavior>
    - Test 1: full draft generation persists accepted sections with 2-4 selected approved source excerpts rather than only generic reference summaries.
    - Test 2: section regeneration returns accepted section output with provenance tied only to the target section, without fabricating provenance on untouched sections.
    - Test 3: route contracts reject attempts to claim grounding when no approved references are active and the workflow falls back to ungrounded generation.
  </behavior>
  <action>Extend `lib/official-document-workflow.ts` so draft generation first selects a small evidence set per section from approved `reference_assets`, active session references, and relevant writing rules, then passes those excerpts into the section prompt and persists them as accepted provenance metadata on the resulting section. Keep the evidence map brownfield-safe: no new retrieval service, no vector store, and no global asset dump. Update `app/api/ai/draft/route.ts` so accepted full-draft and accepted section outputs return provenance-bearing sections and log enough metadata for the existing Phase 2 telemetry path to remain useful. Add direct route tests proving both full and section generation contracts include provenance when eligible and stay provider-free by stubbing the workflow boundary.</action>
  <acceptance_criteria>
    - The workflow persists only selected evidence excerpts, not the entire reference corpus.
    - Accepted full and section draft responses carry section provenance in a stable contract.
    - Section regeneration grounding is scoped to the targeted section and does not imply untouched sections changed.
    - The route contract remains testable without live provider calls.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-03/contracts/grounded-draft-route-contract.test.ts</automated>
  </verify>
  <done>Accepted draft content is grounded in inspectable approved-source evidence instead of generic model-only text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose provenance in version/read models and render it in the existing generate workspace</name>
  <files>app/api/ai/versions/route.ts, app/generate/page.tsx, tests/phase-03/contracts/provenance-read-model-contract.test.ts</files>
  <read_first>
    - .planning/phases/03-grounded-drafting-and-review-trust/03-RESEARCH.md
    - app/api/ai/versions/route.ts
    - app/generate/page.tsx
    - tests/e2e/generate-smoke.spec.ts
  </read_first>
  <behavior>
    - Test 1: version/read-model responses expose accepted provenance metadata without forcing the client to reconstruct it from raw assets.
    - Test 2: `/generate` shows narrow provenance inspection affordances in the draft/review panes while keeping the page structure intact.
    - Test 3: provenance UI gracefully handles sections without grounded evidence instead of rendering misleading empty shells.
  </behavior>
  <action>Update `app/api/ai/versions/route.ts` so listed versions expose the accepted provenance data needed for review and future compare flows. Then add minimal provenance rendering inside `app/generate/page.tsx`: source chips or expandable cards alongside section content in the draft/review panes, with copy kept aligned to the existing Chinese workspace. Do not decompose the full page or move large workflow sections into new modules in this phase. Keep the UI additive and narrow. Add a read-model contract test that proves the response shape and, where practical, source-level UI assertions or compile-safe checks that the page can consume the new data shape without broad churn.</action>
  <acceptance_criteria>
    - Provenance is visible from accepted section content during review.
    - The UI addition is narrow and does not trigger a broad page refactor.
    - Version listing/read models surface the same accepted provenance data the page renders.
    - Empty or ungrounded sections show an honest fallback state rather than fabricated evidence.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --test tests/phase-03/contracts/provenance-read-model-contract.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <done>User can inspect section provenance in the existing workspace and understand which accepted sources informed the current content.</done>
</task>

</tasks>

<verification>
Run the grounded draft and provenance read-model contract tests, then build the app. Inspect the UI changes only as narrow provenance additions inside the current workspace, not as a broad `/generate` decomposition.
</verification>

<success_criteria>
TRUST-01 and TRUST-02 are satisfied when accepted generated sections carry inspectable approved-source provenance and the existing generate workspace can render that provenance clearly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-grounded-drafting-and-review-trust/03-02-SUMMARY.md`
</output>
