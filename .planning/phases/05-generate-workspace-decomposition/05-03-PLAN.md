---
phase: 05-generate-workspace-decomposition
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - package.json
  - app/generate/page.tsx
  - app/generate/_components/stages/DraftStage.tsx
  - app/generate/_components/stages/ReviewStage.tsx
  - app/generate/_components/PendingChangeBanner.tsx
  - app/generate/_components/OperationStatusBanner.tsx
  - app/generate/_components/panels/KnowledgeSidebar.tsx
  - app/generate/_components/panels/VersionHistoryPanel.tsx
  - app/generate/_hooks/useGenerateWorkspaceBootstrap.ts
  - app/generate/_hooks/useGenerateWorkspaceController.ts
  - tests/phase-05/contracts/sidebar-decomposition-contract.test.ts
  - tests/e2e/generate-smoke.spec.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
must_haves:
  truths:
    - "Draft/review behavior, pending-change UX, operation polling, and side-panel helpers all live outside the monolithic page in isolated modules."
    - "Non-critical panel data stays deferred even after sidebar extraction; the phase does not regress to blocking shell readiness."
    - "Seeded browser verification proves the decomposed workspace still honors the trusted workflow behavior established in earlier phases."
  artifacts:
    - path: "app/generate/_components/stages/DraftStage.tsx"
      provides: "Isolated draft-stage editing/regenerate/revise UI"
      contains: "draft-stage controls wired through controller props and operation/pending-change boundaries"
    - path: "app/generate/_components/stages/ReviewStage.tsx"
      provides: "Isolated review/export UI"
      contains: "review controls, export trigger, and policy-check presentation through controller props"
    - path: "app/generate/_components/PendingChangeBanner.tsx"
      provides: "Dedicated compare/accept/reject surface"
      contains: "accept/reject controls outside the page body"
    - path: "app/generate/_components/OperationStatusBanner.tsx"
      provides: "Dedicated durable-operation status surface"
      contains: "queued/running/succeeded/failed messaging outside the page body"
    - path: "app/generate/_components/panels/KnowledgeSidebar.tsx"
      provides: "Deferred non-critical panel owner"
      contains: "phrases, contacts, writing rules, and reference assets in a separate panel module"
    - path: "app/generate/_components/panels/VersionHistoryPanel.tsx"
      provides: "Deferred version-history owner"
      contains: "version list and restore entrypoint outside the page body"
    - path: "tests/phase-05/contracts/sidebar-decomposition-contract.test.ts"
      provides: "Executable proof that draft/review/panel concerns are extracted"
      contains: "imports/strings proving extracted modules own those sections"
  key_links:
    - from: "app/generate/page.tsx"
      to: "app/generate/_components/stages/DraftStage.tsx"
      via: "draft-stage render delegation"
      pattern: "DraftStage"
    - from: "app/generate/page.tsx"
      to: "app/generate/_components/stages/ReviewStage.tsx"
      via: "review-stage render delegation"
      pattern: "ReviewStage"
    - from: "app/generate/page.tsx"
      to: "app/generate/_components/panels/KnowledgeSidebar.tsx"
      via: "sidebar render delegation"
      pattern: "KnowledgeSidebar"
    - from: "app/generate/page.tsx"
      to: "app/generate/_components/panels/VersionHistoryPanel.tsx"
      via: "version panel delegation"
      pattern: "VersionHistoryPanel"
---

<objective>
Finish Phase 5 by extracting the draft/review surfaces and the non-critical side panels, then prove the decomposed workspace still behaves like the trusted pre-refactor workspace while remaining usable before panel helpers finish loading.

Purpose: UX-02 is incomplete until draft/review and panel concerns are no longer buried in one file, and UX-01 must remain true after the extraction rather than regressing during the final cleanup.
Output: Draft/review modules, pending-change and operation banners, deferred sidebar/version panels, and final Phase 5 contract/browser verification.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-generate-workspace-decomposition/05-RESEARCH.md
@.planning/phases/05-generate-workspace-decomposition/05-VALIDATION.md
@.planning/phases/05-generate-workspace-decomposition/05-01-PLAN.md
@.planning/phases/05-generate-workspace-decomposition/05-02-PLAN.md
@app/generate/page.tsx
@app/generate/_hooks/useGenerateWorkspaceBootstrap.ts
@app/generate/_hooks/useGenerateWorkspaceController.ts
@tests/e2e/generate-smoke.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract draft/review plus compare and operation status surfaces</name>
  <files>app/generate/page.tsx, app/generate/_components/stages/DraftStage.tsx, app/generate/_components/stages/ReviewStage.tsx, app/generate/_components/PendingChangeBanner.tsx, app/generate/_components/OperationStatusBanner.tsx, app/generate/_hooks/useGenerateWorkspaceController.ts, tests/phase-05/contracts/sidebar-decomposition-contract.test.ts</files>
  <read_first>
    - .planning/phases/05-generate-workspace-decomposition/05-RESEARCH.md
    - .planning/phases/05-generate-workspace-decomposition/05-02-PLAN.md
    - app/generate/page.tsx
    - app/generate/_hooks/useGenerateWorkspaceController.ts
    - tests/e2e/generate-smoke.spec.ts
  </read_first>
  <behavior>
    - Test 1: draft-stage rendering and controls move into `DraftStage.tsx`.
    - Test 2: review/export rendering moves into `ReviewStage.tsx`.
    - Test 3: pending-change and operation banners are extracted into dedicated components instead of inline page markup.
  </behavior>
  <action>Create `app/generate/_components/stages/DraftStage.tsx` and `ReviewStage.tsx`, plus `app/generate/_components/PendingChangeBanner.tsx` and `OperationStatusBanner.tsx`. Move the current draft/review JSX, compare/accept controls, and durable operation status UI out of `app/generate/page.tsx` while keeping the existing button labels, action order, and controller callback usage intact. Update `tests/phase-05/contracts/sidebar-decomposition-contract.test.ts` so it reads `app/generate/page.tsx` and asserts the page imports these extracted modules instead of rendering those sections inline. Keep all route calls and mutation semantics in `useGenerateWorkspaceController.ts`; the extracted components should stay prop-driven.</action>
  <acceptance_criteria>
    - `app/generate/page.tsx` contains imports for `DraftStage`, `ReviewStage`, `PendingChangeBanner`, and `OperationStatusBanner`.
    - `tests/phase-05/contracts/sidebar-decomposition-contract.test.ts` exists and exits 0.
    - `app/generate/_components/stages/DraftStage.tsx` and `ReviewStage.tsx` do not contain `fetch('/api/`.
    - `app/generate/_components/PendingChangeBanner.tsx` contains `onAccept` and `onReject`, and `OperationStatusBanner.tsx` contains `operationStatus` or `status` props.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --experimental-test-module-mocks --test-concurrency=1 --test tests/phase-05/contracts/sidebar-decomposition-contract.test.ts</automated>
  </verify>
  <done>Draft/review and status-related concerns are no longer inline page responsibilities.</done>
</task>

<task type="auto">
  <name>Task 2: Extract non-critical side panels and run final Phase 5 verification</name>
  <files>app/generate/page.tsx, app/generate/_components/panels/KnowledgeSidebar.tsx, app/generate/_components/panels/VersionHistoryPanel.tsx, app/generate/_hooks/useGenerateWorkspaceBootstrap.ts, app/generate/_hooks/useGenerateWorkspaceController.ts, tests/phase-05/contracts/sidebar-decomposition-contract.test.ts, tests/e2e/generate-smoke.spec.ts, package.json</files>
  <read_first>
    - .planning/phases/05-generate-workspace-decomposition/05-RESEARCH.md
    - app/generate/page.tsx
    - app/generate/_hooks/useGenerateWorkspaceBootstrap.ts
    - tests/e2e/generate-smoke.spec.ts
    - app/generate/_components/stages/DraftStage.tsx
    - app/generate/_components/stages/ReviewStage.tsx
  </read_first>
  <action>Create `app/generate/_components/panels/KnowledgeSidebar.tsx` to own phrases, contacts, writing rules, and reference assets, and `app/generate/_components/panels/VersionHistoryPanel.tsx` to own version-list and restore-entry rendering. Keep these panels wired to deferred data from `useGenerateWorkspaceBootstrap.ts` so the workspace still becomes ready before the panel helpers finish loading. Update `app/generate/page.tsx` to compose the shell, stage modules, banner modules, and panel modules only. Finish Phase 5 verification by extending `tests/e2e/generate-smoke.spec.ts` so the tagged Phase 5 run proves both UX-01 readiness under delayed non-critical endpoints and unchanged seeded authoring behavior after decomposition. If Phase 5 scripts are still missing from `package.json`, add `test:phase-05:contracts` and `test:phase-05:e2e` aliases now.</action>
  <acceptance_criteria>
    - `app/generate/page.tsx` contains imports for `KnowledgeSidebar` and `VersionHistoryPanel`.
    - `app/generate/_hooks/useGenerateWorkspaceBootstrap.ts` still contains deferred fetches for `/api/common-phrases`, `/api/contacts`, `/api/writing-rules`, and `/api/reference-assets`.
    - `package.json` contains `test:phase-05:contracts` and `test:phase-05:e2e` scripts by the end of the phase.
    - Final Phase 5 verification proves delayed panel endpoints do not block shell readiness and that the seeded authoring flow still works after decomposition.
    - `npm run build` succeeds with the fully decomposed workspace file topology.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --experimental-test-module-mocks --test-concurrency=1 --test tests/phase-05/contracts/*.test.ts</automated>
    <automated>npm run build</automated>
    <automated>npm run test:phase-02:rls:reset && npm run test:phase-02:e2e:seed && playwright test --config=playwright.config.ts --project=chromium tests/e2e/generate-smoke.spec.ts --grep "UX-01|UX-02|Phase 5"</automated>
  </verify>
  <done>The generate workspace is decomposed into maintainable modules and still proves critical-path readiness plus trusted workflow behavior.</done>
</task>

</tasks>

<verification>
Run the full Phase 5 contract suite, build, and the tagged seeded Playwright scenario. Confirm the final page is a composition layer rather than a monolith and that deferred side-panel loading still preserves usable first render.
</verification>

<success_criteria>
Phase 5 is complete when the generate workspace is faster to become usable, stage/panel changes can be made in isolated files, and seeded verification proves behavior stayed intact.
</success_criteria>

<output>
After completion, create `.planning/phases/05-generate-workspace-decomposition/05-03-SUMMARY.md`
</output>
