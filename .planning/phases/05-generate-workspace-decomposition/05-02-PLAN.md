---
phase: 05-generate-workspace-decomposition
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - package.json
  - app/generate/page.tsx
  - app/generate/_components/StageNavigation.tsx
  - app/generate/_components/stages/IntakeStage.tsx
  - app/generate/_components/stages/PlanningStage.tsx
  - app/generate/_components/stages/OutlineStage.tsx
  - app/generate/_hooks/useGenerateWorkspaceController.ts
  - lib/generate-workspace.ts
  - tests/phase-05/contracts/stage-module-contract.test.ts
autonomous: true
requirements:
  - UX-02
must_haves:
  truths:
    - "Maintainers can change intake, planning, or outline behavior in isolated files instead of editing the monolithic page."
    - "Stage extraction keeps the controller as the owner of route calls and workflow-state transitions."
    - "The existing visual contract and authoritative workflow semantics remain unchanged while file ownership gets smaller."
  artifacts:
    - path: "app/generate/_components/stages/IntakeStage.tsx"
      provides: "Isolated intake UI surface"
      contains: "intake form controls and intake-specific actions wired through props"
    - path: "app/generate/_components/stages/PlanningStage.tsx"
      provides: "Isolated planning UI surface"
      contains: "planning option generation/selection/confirmation via controller props"
    - path: "app/generate/_components/stages/OutlineStage.tsx"
      provides: "Isolated outline UI surface"
      contains: "outline generation, editing, and confirmation via controller props"
    - path: "app/generate/_components/StageNavigation.tsx"
      provides: "Shared workflow-stage navigation module"
      contains: "step labels and current-step rendering outside the monolithic page body"
    - path: "tests/phase-05/contracts/stage-module-contract.test.ts"
      provides: "Executable proof of stage extraction"
      contains: "imports/strings proving intake, planning, and outline render through stage modules"
  key_links:
    - from: "app/generate/page.tsx"
      to: "app/generate/_components/stages/IntakeStage.tsx"
      via: "intake render delegation"
      pattern: "IntakeStage"
    - from: "app/generate/page.tsx"
      to: "app/generate/_components/stages/PlanningStage.tsx"
      via: "planning render delegation"
      pattern: "PlanningStage"
    - from: "app/generate/page.tsx"
      to: "app/generate/_components/stages/OutlineStage.tsx"
      via: "outline render delegation"
      pattern: "OutlineStage"
---

<objective>
Extract the intake, planning, and outline surfaces into isolated stage modules while keeping all network mutations and workflow-state transitions owned by the shared controller boundary from Plan 05-01.

Purpose: UX-02 is not satisfied until maintainers can change stage-specific behavior without re-entering a 2,894-line page for every edit. This plan should reduce the monolith by responsibility, not by arbitrary JSX chopping.
Output: Stage navigation plus intake/planning/outline modules, controller-owned stage actions/selectors, and a contract test that proves these stages no longer render inline in the page.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-generate-workspace-decomposition/05-RESEARCH.md
@.planning/phases/05-generate-workspace-decomposition/05-VALIDATION.md
@.planning/phases/05-generate-workspace-decomposition/05-01-PLAN.md
@app/generate/page.tsx
@app/generate/_hooks/useGenerateWorkspaceController.ts
@lib/generate-workspace.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract stage navigation plus intake/planning/outline render modules</name>
  <files>app/generate/page.tsx, app/generate/_components/StageNavigation.tsx, app/generate/_components/stages/IntakeStage.tsx, app/generate/_components/stages/PlanningStage.tsx, app/generate/_components/stages/OutlineStage.tsx, tests/phase-05/contracts/stage-module-contract.test.ts, package.json</files>
  <read_first>
    - .planning/phases/05-generate-workspace-decomposition/05-RESEARCH.md
    - .planning/phases/05-generate-workspace-decomposition/05-01-PLAN.md
    - app/generate/page.tsx
    - app/generate/_components/GenerateWorkspaceShell.tsx
    - app/generate/_hooks/useGenerateWorkspaceController.ts
  </read_first>
  <behavior>
    - Test 1: `page.tsx` delegates intake, planning, and outline rendering to extracted stage files.
    - Test 2: Stage navigation exists as a shared component rather than inline JSX.
    - Test 3: Stage-specific files receive props/controller outputs instead of issuing their own route fetches directly.
  </behavior>
  <action>Create `app/generate/_components/StageNavigation.tsx` plus `app/generate/_components/stages/IntakeStage.tsx`, `PlanningStage.tsx`, and `OutlineStage.tsx`. Move the current intake/planning/outline JSX from `app/generate/page.tsx` into those files without changing labels, buttons, or workflow order. Update `tests/phase-05/contracts/stage-module-contract.test.ts` so it reads `app/generate/page.tsx` and asserts the page imports those four extracted modules instead of rendering the stage sections inline. Keep the extracted stage files prop-driven: they may call controller callbacks passed in from the page/shell, but they must not add new direct `fetch('/api/...')` calls.</action>
  <acceptance_criteria>
    - `app/generate/page.tsx` contains imports for `StageNavigation`, `IntakeStage`, `PlanningStage`, and `OutlineStage`.
    - `tests/phase-05/contracts/stage-module-contract.test.ts` exists and exits 0.
    - `app/generate/_components/stages/IntakeStage.tsx`, `PlanningStage.tsx`, and `OutlineStage.tsx` do not contain `fetch('/api/`.
    - `app/generate/_components/StageNavigation.tsx` contains the stage ids `intake`, `planning`, `outline`, `draft`, and `review`.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --experimental-test-module-mocks --test-concurrency=1 --test tests/phase-05/contracts/stage-module-contract.test.ts</automated>
  </verify>
  <done>Intake, planning, and outline are no longer maintained as inline sections inside the monolithic page.</done>
</task>

<task type="auto">
  <name>Task 2: Concentrate intake/planning/outline action wiring inside the shared controller</name>
  <files>app/generate/_hooks/useGenerateWorkspaceController.ts, lib/generate-workspace.ts, app/generate/page.tsx, app/generate/_components/stages/IntakeStage.tsx, app/generate/_components/stages/PlanningStage.tsx, app/generate/_components/stages/OutlineStage.tsx, tests/phase-05/contracts/stage-module-contract.test.ts</files>
  <read_first>
    - .planning/phases/05-generate-workspace-decomposition/05-01-PLAN.md
    - app/generate/_hooks/useGenerateWorkspaceController.ts
    - lib/generate-workspace.ts
    - app/generate/_components/stages/IntakeStage.tsx
    - app/generate/_components/stages/PlanningStage.tsx
    - app/generate/_components/stages/OutlineStage.tsx
  </read_first>
  <action>Extend `app/generate/_hooks/useGenerateWorkspaceController.ts` so it owns the intake, planning, and outline callbacks/selectors currently needed by the extracted stage components, including readiness transitions, planning generation, outline generation, and outline confirmation. Keep `lib/generate-workspace.ts` as the authoritative place for hydration/save helpers and add only pure helper extraction there if the controller needs shared selectors or stage-specific derived state. Update the stage modules so they consume controller props rather than reconstructing workflow state locally. Keep `app/generate/page.tsx` as composition glue only: it should choose which stage component to render and pass through controller outputs, not re-embed the old stage-level business logic.</action>
  <acceptance_criteria>
    - `app/generate/_hooks/useGenerateWorkspaceController.ts` contains the callbacks `handleIntake`, `handleGeneratePlanning`, `handleGenerateOutline`, and `handleConfirmOutline`.
    - `app/generate/page.tsx` no longer contains `currentStep === 'intake'`, `currentStep === 'planning'`, or `currentStep === 'outline'`.
    - `app/generate/_components/stages/IntakeStage.tsx`, `PlanningStage.tsx`, and `OutlineStage.tsx` export named stage components and do not contain `fetch('/api/`.
    - `npm run build` succeeds after the controller/stage extraction.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --experimental-test-module-mocks --test-concurrency=1 --test tests/phase-05/contracts/stage-module-contract.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Stage-specific behavior is isolated without duplicating controller logic across multiple files.</done>
</task>

</tasks>

<verification>
Run the Phase 5 stage-module contract and build. Confirm `page.tsx` is now composition-only for intake/planning/outline and that the controller remains the owner of route/mutation semantics.
</verification>

<success_criteria>
UX-02 is materially improved once the first three workflow stages live in isolated modules and the page no longer owns their full inline render + action logic.
</success_criteria>

<output>
After completion, create `.planning/phases/05-generate-workspace-decomposition/05-02-SUMMARY.md`
</output>
