---
phase: 05-generate-workspace-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/generate/page.tsx
  - app/generate/_components/GenerateWorkspaceShell.tsx
  - app/generate/_hooks/useGenerateWorkspaceBootstrap.ts
  - app/generate/_hooks/useGenerateWorkspaceController.ts
  - lib/generate-workspace.ts
  - tests/phase-05/contracts/workspace-bootstrap-contract.test.ts
  - tests/e2e/generate-smoke.spec.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
must_haves:
  truths:
    - "The generate workspace becomes interactive from a critical boot path that does not wait for non-critical side-panel endpoints."
    - "The first Phase 5 cut creates a reusable controller/bootstrap boundary instead of keeping all load orchestration inline in `app/generate/page.tsx`."
    - "Phase 5 verification proves delayed panel endpoints do not block the main drafting shell."
  artifacts:
    - path: "app/generate/_hooks/useGenerateWorkspaceBootstrap.ts"
      provides: "Critical bootstrap plus deferred panel loading boundary"
      contains: "critical settings/draft hydration and deferred phrases/contacts/rules/assets/version loading"
    - path: "app/generate/_hooks/useGenerateWorkspaceController.ts"
      provides: "Shared workspace controller boundary"
      contains: "controller-owned state selectors, hydration entrypoints, and mutation wiring reused by later plans"
    - path: "app/generate/_components/GenerateWorkspaceShell.tsx"
      provides: "Stable top-level shell for stage rendering and panel composition"
      contains: "boot/loading/error shell props rather than inline page-level branching"
    - path: "tests/phase-05/contracts/workspace-bootstrap-contract.test.ts"
      provides: "Executable proof that the shell/controller boundary exists and the page no longer owns the blocking six-request bootstrap"
      contains: "imports/strings proving deferred panel loading and controller adoption"
    - path: "tests/e2e/generate-smoke.spec.ts"
      provides: "Seeded UX-01 readiness proof"
      contains: "Phase 5 tagged scenario that delays non-critical endpoints and asserts the main workspace is usable first"
  key_links:
    - from: "app/generate/page.tsx"
      to: "app/generate/_hooks/useGenerateWorkspaceBootstrap.ts"
      via: "top-level workspace boot orchestration"
      pattern: "useGenerateWorkspaceBootstrap"
    - from: "app/generate/page.tsx"
      to: "app/generate/_components/GenerateWorkspaceShell.tsx"
      via: "top-level shell composition"
      pattern: "GenerateWorkspaceShell"
    - from: "tests/phase-05/contracts/workspace-bootstrap-contract.test.ts"
      to: "app/generate/page.tsx"
      via: "source-level contract assertions"
      pattern: "useGenerateWorkspaceBootstrap|GenerateWorkspaceShell"
---

<objective>
Create the Phase 5 foundation by separating critical workspace boot from deferred panel loading and by introducing the controller/shell boundaries that later decomposition plans can build on safely.

Purpose: UX-01 is blocked until `/generate` stops waiting on phrases, contacts, rules, reference assets, and version history before the main drafting shell can hydrate. UX-02 also needs an architectural seam before stage and panel JSX can be extracted cleanly.
Output: Phase 5 bootstrap/controller hooks, a stable shell component, package scripts/tests for Phase 5, and a delayed-endpoint browser proof.
</objective>

<execution_context>
@/Users/agent/.codex/get-shit-done/workflows/execute-plan.md
@/Users/agent/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-generate-workspace-decomposition/05-RESEARCH.md
@.planning/phases/05-generate-workspace-decomposition/05-VALIDATION.md
@app/generate/page.tsx
@lib/generate-workspace.ts
@tests/e2e/generate-smoke.spec.ts
@package.json

<interfaces>
Current blocking boot path in `app/generate/page.tsx`:

```typescript
const [settingsRes, phrasesRes, contactsRes, draftsRes, rulesRes, assetsRes] = await Promise.all([
  fetch('/api/settings'),
  fetch('/api/common-phrases'),
  fetch('/api/contacts'),
  fetch('/api/drafts'),
  fetch('/api/writing-rules'),
  fetch('/api/reference-assets'),
]);
```

Critical-only target:

```text
Critical: settings + selected draft hydration + provider resolution
Deferred: common phrases, contacts, writing rules, reference assets, versions
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 5 verification harness for deferred bootstrap readiness</name>
  <files>package.json, tests/phase-05/contracts/workspace-bootstrap-contract.test.ts, tests/e2e/generate-smoke.spec.ts</files>
  <read_first>
    - .planning/phases/05-generate-workspace-decomposition/05-RESEARCH.md
    - .planning/phases/05-generate-workspace-decomposition/05-VALIDATION.md
    - tests/e2e/generate-smoke.spec.ts
    - package.json
    - app/generate/page.tsx
  </read_first>
  <behavior>
    - Test 1: Phase 5 has a contract test that proves `app/generate/page.tsx` delegates bootstrap/shell responsibilities to extracted boundaries.
    - Test 2: The seeded browser suite contains a Phase 5-tagged scenario that delays one or more non-critical endpoints and still reaches a usable main workspace before the panel data resolves.
    - Test 3: `package.json` exposes stable Phase 5 verification commands rather than requiring ad hoc one-off commands.
  </behavior>
  <action>Add `tests/phase-05/contracts/workspace-bootstrap-contract.test.ts` as a source-level contract suite that reads `app/generate/page.tsx` and asserts the page imports `GenerateWorkspaceShell` and `useGenerateWorkspaceBootstrap`, while the blocking six-request `Promise.all([... '/api/settings', '/api/common-phrases', '/api/contacts', '/api/drafts', '/api/writing-rules', '/api/reference-assets' ...])` no longer exists in the page file. Extend `tests/e2e/generate-smoke.spec.ts` with a Phase 5-tagged scenario that routes `/api/writing-rules` and `/api/reference-assets` through artificial delay, then proves the current draft shell, current workflow stage, and one core action control are visible before those delayed endpoints finish. Add Phase 5 scripts to `package.json` for the contract suite and the tagged browser run so later plans can reuse stable commands.</action>
  <acceptance_criteria>
    - `tests/phase-05/contracts/workspace-bootstrap-contract.test.ts` exists and exits 0.
    - `package.json` contains a `test:phase-05:contracts` script.
    - `tests/e2e/generate-smoke.spec.ts` contains a Phase 5-tagged UX-01 scenario and explicit delays for `/api/writing-rules` and `/api/reference-assets`.
    - `tests/e2e/generate-smoke.spec.ts` does not add artificial delay for `/api/settings`.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --experimental-test-module-mocks --test-concurrency=1 --test tests/phase-05/contracts/workspace-bootstrap-contract.test.ts</automated>
  </verify>
  <done>Phase 5 has an executable readiness-proof harness before any large refactor lands.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce controller and shell boundaries, then defer non-critical panel loading</name>
  <files>app/generate/page.tsx, app/generate/_components/GenerateWorkspaceShell.tsx, app/generate/_hooks/useGenerateWorkspaceBootstrap.ts, app/generate/_hooks/useGenerateWorkspaceController.ts, lib/generate-workspace.ts, tests/e2e/generate-smoke.spec.ts, package.json</files>
  <read_first>
    - .planning/phases/05-generate-workspace-decomposition/05-RESEARCH.md
    - app/generate/page.tsx
    - lib/generate-workspace.ts
    - tests/e2e/generate-smoke.spec.ts
    - tests/phase-01/generate-workspace-contract.test.ts
  </read_first>
  <action>Create `app/generate/_hooks/useGenerateWorkspaceBootstrap.ts` for the first-load path and `app/generate/_hooks/useGenerateWorkspaceController.ts` for controller-owned workspace state/actions. Create `app/generate/_components/GenerateWorkspaceShell.tsx` so `app/generate/page.tsx` becomes a thin composition file that wires the shell plus controller/bootstrap outputs together. Move the critical boot path to `settings` plus draft selection/hydration and provider resolution. Move `/api/common-phrases`, `/api/contacts`, `/api/writing-rules`, `/api/reference-assets`, and version-history loading to deferred loaders that update their respective panel state after the main shell is ready. Preserve the authoritative helper boundary by continuing to use `buildDraftSaveRequest(...)` and `deriveHydratedDraftView(...)` from `lib/generate-workspace.ts`, and do not change any server route payload contracts in this plan.</action>
  <acceptance_criteria>
    - `app/generate/page.tsx` contains `GenerateWorkspaceShell`, `useGenerateWorkspaceBootstrap`, and `useGenerateWorkspaceController` imports.
    - `app/generate/_hooks/useGenerateWorkspaceBootstrap.ts` contains deferred fetches for `/api/common-phrases`, `/api/contacts`, `/api/writing-rules`, and `/api/reference-assets`.
    - `lib/generate-workspace.ts` still exports `buildDraftSaveRequest` and `deriveHydratedDraftView`.
    - The delayed-endpoint browser proof passes while the main workspace shell still renders before non-critical panel data completes.
    - `npm run build` succeeds after the shell/controller split.
  </acceptance_criteria>
  <verify>
    <automated>node --experimental-strip-types --experimental-test-module-mocks --test-concurrency=1 --test tests/phase-05/contracts/workspace-bootstrap-contract.test.ts</automated>
    <automated>npm run build</automated>
    <automated>npm run test:phase-02:rls:reset && npm run test:phase-02:e2e:seed && playwright test --config=playwright.config.ts --project=chromium tests/e2e/generate-smoke.spec.ts --grep "UX-01|Phase 5"</automated>
  </verify>
  <done>The workspace has a reusable shell/controller foundation and no longer blocks core readiness on side-panel data.</done>
</task>

</tasks>

<verification>
Run the Phase 5 bootstrap contract, build, and the delayed-panel seeded browser proof. Confirm the main workspace becomes usable before non-critical panel endpoints finish and that no server route contract changed.
</verification>

<success_criteria>
UX-01 is on track once the shell boots from critical data only, and UX-02 has a safe foundation once the page delegates orchestration to extracted controller/bootstrap boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/05-generate-workspace-decomposition/05-01-SUMMARY.md`
</output>
